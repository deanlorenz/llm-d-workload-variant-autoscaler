# Second topic

Fixture file. Exists only so one convention name is defined in two files.

### convention: doubly-defined
description: Defined here and in first.md, which must be an error, not a choice.
scope: fixture only
trigger: conv or conv-lint scanning this directory
status: active
origin: fixture for the duplicate-name path

Body prose.
