description: this file deliberately omits the '### convention: <name>' marker.
scope: conv-edit test
trigger: n/a
status: active
origin: n/a

conv-edit must refuse this rather than silently reusing the old marker.
