to: a new dedicated session (Dean is opening one, 2026-08-12)
reason: re-read plan
refs:
  - planning/autoscaling-viz-design.md — the Type 1 design for the viz toolchain, Status: DRAFT —
    awaiting Dean's review. This session IS that review, scoped to the panels specifically. Read the
    TOC first, then § "Panel design" (L300:331) and § "Known gap: no scaling-decision-reason panel
    exists" in full — those are the direct target of "comments about the panels." The rest of the doc
    (estimation model, regime-decomposition critique, the three open capacity-model questions) is
    background, fetch on demand per its own Reading Protocol rather than reading cover to cover.
  - planning/ta-pokprod-campaign-20260810-results.md § "Missing: a scaling-decision panel" — the
    original ask, restated: a bottom panel showing WVA's logged scaling *reasons* (the
    `P1-obs`/`P2-hist`/`P3-k2`/`P4-k1` capacity-source codes and `scaling-decision` log lines),
    aligned on the x-axis with panel 2's replica trace, instead of requiring a hand-grep of
    `controller.log` for each finding.
  - autoscaling-viz/render_real_trace.py — the actual renderer, six panels (1a/1b/2/3/4/5) drawn from
    a bundle.json. This is the code the review is about.
  - plans/scratch/campaign-20260810-viz/ or the canonical benchmark/runs/<id>/viz/ locations (per
    ta-pokprod-architecture-design.md §4) — existing rendered panels from the 2026-08-10 campaign,
    already-gathered data this session can work from directly without a new extraction or a cluster
    run.
note: Dean, 2026-08-12 — "I want to start a plan session on the visualization only -- comments I had
  about the panels -- can work off data already gathered." This is scoped narrower than the full
  viz-design review: panels specifically, not the estimation-model questions (those stay open,
  tracked separately in the design doc's own § "Open design questions"). No new data collection is
  implied or needed — the campaign's rendered panels and their bundle.json/coverage.json already
  exist and are the working material.
