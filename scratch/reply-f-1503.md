Thanks — good catch. Confirmed the bug: `worstFreshnessStatus` gated the *age* update on `hasTimestamp` but not the *severity* comparison, so an absent-by-design metric (no EPP, prefix caching off) always won the max via its `"missing"` classification and masked a genuinely stale driving metric from the `sanity.go` stale-metrics gate. `27523605`'s fix (skip absent timestamps entirely, fall back to `"missing"` only when none are present) is correct and minimal, and the two new regression tests exercise exactly the fixed branch. Happy to keep it on this branch.
