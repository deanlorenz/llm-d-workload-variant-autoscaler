# TA on pokprod — Campaign Report

> **RELOCATED 2026-08-15 (D-53).** This doc now lives on the `benchmark` branch at
> `benchmark/docs/benchmark-reports/ta-pokprod-campaign-report.md` — Dean's call: it's Type-6/
> PR-bound "guide" material describing what was tested, found, and gapped, not internal `plans`
> tracking. Its `../../benchmark/runs/...` links only resolved because it lived on `plans` inside
> the same local checkout; moving it into `benchmark/` turns those into same-worktree relative
> links that resolve correctly on GitHub/clone, closing that gap. Read the new location for
> current content — this stub is kept only so old citations by section/finding number, and the
> two docs this report itself superseded (`ta-pokprod-campaign-20260810-results.md`,
> `ta-pokprod-rerun-results-20260813.md`), still resolve their own links to this filename.
