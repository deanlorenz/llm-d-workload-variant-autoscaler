# TA 0.9 rebase onto `827c8542` — supersedes `ta-09-rebase-ef28744b.md`

**Type 3 (ephemeral).** Delete after A′/C/D are verified and force-pushed.
**Author:** plan-agent. **Date:** 2026-07-28.
**Supersedes:** `planning/ta-09-rebase-ef28744b.md` (target `ef28744b` is now 2 commits
behind; A merged from it, D conflicts against it).

## Why (what changed since the ef28744b rebase)

After the four branches were rebased onto `ef28744b` and force-pushed, upstream `main`
advanced twice:

- `31fd0f84` — **#1452** priority-weighted GPU rescale under contention (Alpha).
- `827c8542` — **#1478 = our PR A** (ta-devguide-fixes), **now MERGED** and main's tip.

Consequences:

- **A #1478 — MERGED. Out of scope.** Do not touch `ta-devguide-fixes`.
- **A′ #1479 — clean merge, mergeable, CI green** (against the old `ef28744b` base).
  Rebasing onto `827c8542` for uniformity + to run CI against real latest main. Its only
  overlap with new main is a doc file (`throughput-analyzer.md`), disjoint hunks →
  expected clean rebase.
- **C #1480 — textually clean merge**, but shares `internal/engines/saturation/engine.go`,
  `internal/collector/registration/throughput_analyzer.go`, and `throughput-analyzer.md`
  with #1452's changes. Clean text ≠ clean build. Rebase + **rebuild** to catch any
  semantic/build break against #1452's `engine.go`.
- **D #1481 — CONFLICTING.** Real conflict in
  `internal/engines/pipeline/analyzer_helpers.go` with #1452 (detail below). GitHub could
  not build a merge ref, which is why D's `lint-and-test` never ran (it was never a
  webhook drop). Rebase + resolve.

The tip-amend on D (`1df0f871`) is now moot — the rebase changes the SHA again anyway.

## Branches — rebase `--onto 827c8542 ef28744b`

| Br | Branch | PR | pre-rebase tip | # commits | expectation |
|----|--------|----|----------------|-----------|-------------|
| A′ | `ta-registration-safety` | #1479 | `2f07ca43` | 5 | clean rebase |
| C  | `ta-model-level-demand`  | #1480 | `60b93643` | 4 | clean rebase; rebuild-critical |
| D  | `ta-veto-liveness`       | #1481 | `1df0f871` | 6 | **conflict in analyzer_helpers.go** |

Command per worktree:
```
git rebase --onto 827c8542 ef28744b
```
(A′/C base is `ef28744b`; D's base is also `ef28744b` — the amend didn't change the base.)

## D conflict — `internal/engines/pipeline/analyzer_helpers.go`

Both sides insert new top-level declarations at the **same location** (immediately after
the import block, before `applyAllocation`):

- **#1452** adds imports `maps`, `slices` and a `rolesOf(vcs []domain.VariantCapacity)`
  helper.
- **D** adds the `analyzerReasonNoData`/`analyzerReasonError` consts and the
  `ResultIsInformative(nr NamedAnalyzerResult) bool` function.

**Resolution:** keep **both** — #1452's imports + `rolesOf`, and D's consts +
`ResultIsInformative`. They are independent; just place them adjacently and preserve both
import additions.

**Semantic check the coder MUST make (not just the textual merge):** #1452 reworked the
scale-down / rescale path, and its RC-2 (flagged in review [`planning/PR1452-review.md`])
was precisely that its reclaim path *bypasses the multi-analyzer scale-down gate*. D's
entire purpose is to gate that scale-down veto on analyzer liveness — D modifies
`safeRemovalReplicasForRole`, `applyDeallocationForRole`, and `needsScaleDownForRole` to
skip non-live analyzers. So:

1. Determine whether #1452 also modified `needsScaleDownForRole` /
   `safeRemovalReplicasForRole` / `applyDeallocationForRole` (run
   `git show 827c8542 -- internal/engines/pipeline/analyzer_helpers.go` and
   `git log ef28744b..827c8542 -- internal/engines/pipeline/analyzer_helpers.go`).
2. If #1452 left those functions untouched → D's Live-gating hunks apply cleanly on top;
   only the top-of-file insertion needs manual merge.
3. If #1452 changed any of those functions → **re-apply D's Live-gate on top of #1452's
   version**, preserving D's behavior (non-live analyzer cannot veto scale-down / cannot
   constrain the safe-removal minimum). Do **not** silently drop or alter D's gate to make
   the merge simpler.
4. **Flag, don't fold:** if resolving the conflict forces any change to D's liveness-gate
   behavior (or to #1452's rescale behavior), that is its own decision point — call it out
   in the handoff as a distinct item, not buried in "resolved the conflict." (Same
   discipline as the semantic-pivot rule.)

## Verification checklist (per branch, after rebase, before done)

1. **Per-file diff inventory** — for each file the branch touches that also moved on the
   new base, `git diff <pre-rebase-tip> HEAD -- <file>` and confirm every behavior claimed
   in the branch's commit messages survives. Files that moved on main (`ef28744b..827c8542`)
   and are touched by our branches: `analyzer_helpers.go` (D), `engine.go` (C, D),
   `throughput_analyzer.go` (C), `throughput-analyzer.md` (A′, C).
2. **Per-commit message-vs-diff check** — `git log --stat` the rebased range; no commit
   silently lost a hunk to the three-way merge.
3. **Full gates** against the new base (now includes #1452's engine.go):
   - `make test` — PASS
   - `gofmt -l ./internal/... ./pkg/... ./cmd/...` — empty
   - `make lint` — clean
   - `go build ./...` — clean
   - `-race` on **D** (liveness/keying): `go test -race ./internal/engines/saturation/...
     ./internal/engines/pipeline/...`
   - **C specifically:** confirm `go build ./...` and `make test` are clean against #1452's
     `engine.go` (the rebuild-critical case).
   - **D specifically:** re-run the liveness tests (`engine_v2_liveness_test.go`,
     `analyzer_helpers_test.go`, `engine_queueing_model_test.go`) and confirm the
     scale-down veto still respects liveness after the merge with #1452's rescale path.
4. **DCO** — `git log 827c8542..HEAD --format="%b" | grep -c Signed-off-by` equals commit
   count; every commit carries `Signed-off-by: Dean H Lorenz <dean@il.ibm.com>`.

## Out of scope for the coder

- **No push.** Report new tips + (for D) the conflict-resolution summary and any flagged
  semantic decision via handoff; Dean force-pushes each with `--force-with-lease` after
  per-branch PR confirmation.
- **No PR-body / CURRENT.md edits.**
- **Do not touch A / `ta-devguide-fixes`** (merged).

## Post-rebase (plan-agent / Dean, not the coder)

- Force-push A′/C/D `--force-with-lease` (Dean-confirmed, per-branch PR warning).
- Verify each PR's `lint-and-test` re-dispatches and goes green (D should now get a merge
  ref once it's conflict-free).
- Update the four PR bodies (drop the stale #1477 caveat — A already merged) and CURRENT.md
  (A MERGED; #1452 merged; A′/C/D rebased onto `827c8542`).
- Delete this plan doc + the superseded `ta-09-rebase-ef28744b.md` once verified.
