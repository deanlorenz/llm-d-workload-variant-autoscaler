# PENDING Type-3 edits — NOT APPLIED, NOT A SPEC

> ⚠️ **This is not a plan document and carries no authority.** It is the planner's manifest of edits
> *proposed* to `ta-anchor-dynamic-refresh-plan.md` and awaiting Dean's single approval. Nothing here
> has been applied to any plan, and no coder should read scope from it — the authoritative Type 3 is
> [`ta-anchor-dynamic-refresh-plan.md`](ta-anchor-dynamic-refresh-plan.md) @ `1a116e7a`, and the Type 1
> [`combined-analyzer-optimizer-design.md`](combined-analyzer-optimizer-design.md) (FINAL, frozen
> @ `8c2a9b04`) governs it on disagreement. Delete this file once the batch lands.

**Why it exists:** the batch grew 17 → ~23 sites across a long session that has compacted twice.
Recording it protects against exactly the silent content loss CONVENTIONS warns about, and lets Dean
review the batch as a list rather than as prose spread over many turns.

**Target file for every row unless stated:** `planning/ta-anchor-dynamic-refresh-plan.md`.
`scripts/toc-refresh.sh` runs **last**, after all rows are applied.

---

## A. Unconditional — apply on batch approval

| # | Site | Change | Origin |
|---|---|---|---|
| A1 | `:288` | C11 ranking claim is **inverted**: `Cost = 0` ⇒ the sentinel sorts **first**, not last | my verification |
| A2 | `:1315-1316` | same inversion | " |
| A3 | `:1324` | same inversion | " |
| A4 | `:1608-1612` | same inversion | " |
| A5 | `:2199-2200` | same inversion | " |
| A6 | `:288` | Cap placement: the one-replica ceiling must sit at the **granting site**, never in the `MaxReplicas` headroom branch. Widened from one site to **three**, all verified at `eb12089a`: `cost_aware_optimizer.go:104-111` (the `return …, math.MaxInt` at `:111` is *outside* the `MaxReplicas != nil` block); `greedy_score_optimizer.go:711-717` (clamp wholly inside the nil-guard); `rescale.go:454-460` (worst — a `for wantGPUs-spent >= g` loop whose **only** exit is a `break` guarded by `MaxReplicas != nil && > 0`, so an unset ceiling means replicas are granted until the GPU budget is exhausted). `MaxReplicas` is `*int` and nil/`0` are treated alike as unbounded, and the sentinel's population is never-seen zero-replica variants — the least likely to be tuned | my verification, then reviewer F42 independently at all 3 sites |
| A20 | A1–A5 addendum | The ranking inversion's **fix is `N5`, not a different sentinel value**: a never-measured variant's `Cost` arrives as `0` from the *same* zero-replica lookup that leaves `AcceleratorName` empty, so the ratio is `0/1` and it sorts first. `PRC = 1` stays the right choice and the property recovers with **no change to C11** once that lookup is fixed. Ties A1–A5 to the `N5` item currently listed as out-of-scope | coder, C11 impl |
| A19 | §C11 | Record the **post-freeze ordering dependency**: C6e's `firstDraw` floor at `greedy_score_optimizer.go:702` *raises* `capN` after `replicasToCover` at `:701` and before both bounds (`:710` pool, `:711-717` headroom). A ceiling placed next to `replicasToCover` — the natural reading of the Type-1 instruction — is therefore overwritten on the first draw. Single-role case is benign (`:702` raises to exactly 1 = the ceiling); the breach needs two roles resolving to the same sentinel variant in one pre-commit window, which **neither the reviewer nor I have established as reachable**. This is why the clamp shape is `min(cap, 1 - targets[v])` and not `min(cap, 1)` | reviewer F43 + my verification |
| A7 | §C6c items 2/3/4 | corrections carried from the currency-pivot read | my verification |
| A8 | §C6d items 1/1b/2 | ditto | " |
| A9 | §4 | Commit-message reword cost is **13**, not nine or ten: 14 commits, 13 token-bearing, 10 token-bearing subjects, `34b18bc5` the only clean one. Supersedes the "9 now vs 16 later" framing | reviewer F-recount |
| A10 | §4a | Code/doc token locations ~**49**, not 32. New load-bearing fact: production `.go` doc comments went **0 → 19** at base `075a208e`, so **PR-2 is the first to reach production prose** (PR-1 leaked into test comments only) | reviewer F-recount |
| A11 | `:1630-1632` | The C6e golden rule ("goldens are expected to move, incl. `[sat]`-only P/D") is **structurally impossible** — with one active model `allocationMean = 0` so `target == claim`, and `claimGPUs` sums role claims, so the entitlement equals combined spend exactly. Sharper replacement: **no single-model golden *can* move**; the entitlement bug needs multi-model contention | coder + reviewer, independently; reviewer retracted his own contrary pre-registration |
| A12 | `:284` | "the pool was enforced, the fair share was not" is **half true** — in `allocateForModelPaired` the pre-C6e code drove the pool to **−5** on a 7-GPU pool, so the pool was not enforced there either | coder item 0 counterfactual |
| A13 | `:1555` | same correction | " |
| A14 | `:1753` | same correction | " |
| A15 | new §-entry | **DEFERRED** classification for the pool double-count (`analyzer_helpers.go:846-857`: `pick()` runs per role against the same un-decremented `available`; decrement only at the commit loop `:903`). Needs: what it did, why deferred, both candidate fix shapes (speculative decrement + rollback vs shrinking copy reconciled at commit). **Live, not latent**, in the pool-bound case | my correction of the coder's framing |
| A16 | §-note | The indivisible-unit floor now exists at **three** landed sites, each citing the others: `greedy_score_optimizer.go:458-460` (`bound = prc`), `:694` (`firstDraw && capN < 1`), `:822` (`math.Ceil` in `replicasToCover`). Any "floor everywhere" mandate applied literally now means reverting three sites, not one expression | my verification |
| A17 | §C9 | **Finding 29** — the new "Fair-share iteration" paragraph says the single-model case "gets `mean == 0`". It is **`allocationMean`** that is zeroed (`:292-297`); `mean` equals that model's own remaining and still governs the `w.remaining > mean` drop check at `:308`. As written it reads as the inverse | reviewer F29 |
| A18 | §C6f or §C11 | Give the unplanned dormant spec `537b0153` (test-only, +88, `PIt`, asserts the honest even split) a **§-home**, or an explicit "unplanned — revert if the claim-pricing disposition rejects it" note. It currently says in its own first line that it is not in the plan | reviewer, endorsing it |

| A21 | §2e.2 `:1169-1170` | The import-cycle clearance is **false**. *"`internal/config` imports no `internal/engines` package"* ignores `internal/config/config_test.go`, which is **`package config`** and imports `throughput` for the drift guard on the duplicated `throughputAnalyzerName` literal (`config.go:338-341`). So `throughput → config` is a **test-binary** cycle: `go build ./...` stays green, `go test ./internal/config/...` fails. The text points away from its own cause. C10 **landed** as `1a50b418`, and the shipped shape is **not** any of Finding 48's four options — it is a fifth: `resolveKSat(cfg domain.AnalyzerConfig)` (`analyzer.go:217-223`) asserts a **self-declared single-method interface** `interface{ KSat() float64 }` against **TA's own already-injected config parameter** — the one TA received and previously ignored. Production `throughput` imports `internal/config` **nowhere** (verified: the only two matches are prose in comments); the sole importer is `k_sat_test.go`, which is in-package `package throughput`, and both test binaries stay acyclic because test files only ever compile into their own package's binary. **Correct transcription for §2e.2** (all three clauses, none of which the current text says): production `throughput` never imports `internal/config`; the accessor is a self-declared single-method interface on the config TA already receives; the duplicated `0.80` is drift-guarded from **both** sides — `config.go:338-341` in one direction and the new `TestFallbackKSatMatchesConfigDefault` (`k_sat_test.go:24-27`) in the other. Still **explicitly rule out deleting the drift guard**: §4b-classifiable, presents as a build fix. **Dean, 2026-08-08 — the layering question ("may an analyzer read another analyzer's config section?") is settled as LEAVE-AS-SHIPPED + record a TODO, not a decision**; the natural host already exists in code (`analyzer.go:214-216`, `TODO: unify with the system-wide k_sat used by the EPP`) and matches the CURRENT Issues-to-Open entry, so no new note is needed — only the plan text | reviewer F48 diagnosis (stands, and deserves the credit); designer retraction of its own option-3 recommendation; shipped shape + acyclicity verified by me at `1a50b418` |
| A21b | §2e.2 — Dean's missing-in-sat question, answered | Record the **degenerate-case table**, because the answer is "0.80 in every case, by two independent routes that cannot silently drift". No sat config at all (nil, or any config type without `KSat()`) → assertion fails → `fallbackKSat` = **0.80**. `saturation: {}` with `kvCacheThreshold` unset → `ApplyDefaults:294-295` writes `DefaultKvCacheThreshold` → **0.80**. Explicit `0` → treated as unset by `ApplyDefaults`, and even if it reached `resolveKSat` the `k > 0` guard rejects it → **0.80**. Negative → `Validate:401-402` rejects the config outright, and `k > 0` is the backstop → **0.80**. **`> 1` → `Validate:401-402` rejects** (*"kvCacheThreshold must be between 0 and 1"*), so the nonsense-fraction path Dean was implicitly worried about cannot reach TA at all; `:418` additionally requires `KvCacheThreshold >= KvSpareTrigger`. So no configuration-hygiene workaround is needed — every degenerate path is closed in code. One honest behavior note for the commit message: the **no-config path changes `0.85 → 0.80`** (TA never read config before, so it was always the old `DefaultKSat`), which is inside the already-recorded sub-1% band, **not** the "~6%" figure | my verification of `resolveKSat`, `constants.go:52-68`, `saturation_scaling.go:294-295,:401-402,:418` |
| A22 | throughout §2/§2d/§2f | Tip + status are stale by nine commits: `d9f3b97e` → **`b6bb525c`**, and *"C6c has zero edits, scoped read-only, held on Dean's call"* → C6c **landed** as `34b18bc5` (GPU-space pivot), then C6d `330fcd26`, C6e `784c2b5c`, C6f `a679f2ad`, C11 `b6bb525c`, C10 in flight. Every *"as of `d9f3b97e`"* line-reference label needs re-verification — the drift is actual, not prospective, and C6c–C6f touched exactly those files | PR-1 reviewer Part 2.1 + my verification |
| A23 | §rebase step | Must specify **`git rebase --onto upstream/main 075a208e`**. PR-1 squash-merged, so PR-2's merge-base is `aadaa596` (pre-PR-1) and a bare `git rebase upstream/main` sees all of PR-1 as added on both sides: **10 conflicts**, incl. `add/add` on `optimizer_combine_characterization_test.go` + `optimizer_scale_from_zero_test.go`. True surface is **2**: `analyzer_helpers.go` (PR-2's new comment vs `main`'s `buildCapacityMap` swap — take both) and `rescale.go` (**duplicate** `if anchor == nil` guard, arrived at independently). Preferred `rescale.go` resolution: take **`main`'s**, which is §4a-clean, so resolving the conflict removes a violation. Also record that local `ta-anchor-refactor-v2@075a208e` is **not** what merged — `a38d7b73`'s four fixes exist only on `origin/` and in `main` | PR-1 reviewer Part 4, measured via `git merge-tree` |
| A24 | §4 / §4a — supersedes A9/A10 | **11 of 17** commit messages carry a token (heading to ~14), in-tree count `1 → 22 → 36` across C6c–C6f. Critically, the plan's *"17 already inherited at `075a208e`, out of scope"* is **1** under a tighter grep: the plan's expression also catches **legitimate code identifiers** — the `T1-ols` reason string, and `V1`/`V2`, which appear ~12× in `internal/config/saturation_scaling.go` alone as saturation **engine-version** names — which must not be renamed. The two figures were never comparable and more of the debt is PR-2's own than credited. **Pin one grep expression in the plan before re-quoting any count** | PR-1 reviewer Part 5 |
| A24b | A24 addendum — the ledger to pin | The reviewer's third recount is the one to adopt as the pinned expression, and it is now **self-corrected twice**: pattern `C<n>[a-f]?` · `PR-1/2` · `W<n>` · `N<n>` · `U<n>` · `D-a/D-b` · `T1.<n>` · `FZ-admission`, text files only (`git grep -I`), over `internal/**` + `docs/**`. Table: `075a208e` **7** inherited · `b6bb525c` **52** / 16-of-18 messages · `1a50b418` **53** / 17-of-19 · `79a590d6` **54** / **18-of-20**. Its own retractions: it had quoted 53 "as of `b6bb525c`" (was 52) and 17-of-19 there (was 16-of-18), and an earlier ~61 included **8 `Binary file … matches` lines from the dev-guide PNGs**. Two reasons to prefer it over my own figures: `-I` kills the PNG class, and I verified its pattern is **clean of the golden-name false positives A24 warns about** — no `C1`/`A1`–`A4` exists anywhere in `internal/**` at this tip, and `V1` is outside the pattern. **But it is not a closed figure either**, and this is what the plan must say: 54 is **token-class only**, under a pattern that excludes both the plans-branch **path/filename** class (the reviewer acknowledges it separately in its own §5 and leaves it unquantified) and bare **"Type-1"** prose — so A28's two surviving violations sit outside all 54, one of them added by C11. Pin the expression *and* state what it does not count | reviewer's C9-sweep handoff §1, verified by me at `79a590d6` |
| A25 | §2f | Steer C11's `Reason` constant spelling away from `a38d7b73`'s new `variant.ReasonOptimizationRefused` / `constants.K8SEventOptimizationRefused` family — different namespace, no technical collision, but two "Reason" families now coexist in-tree and §2f leaves the spelling to the coder. Optionally cite `a38d7b73`'s own `allPicked`-clears-and-breaks explanation as **independent corroboration** in merged code of §2f's ⚠ skip-not-zero-cap requirement | PR-1 reviewer Part 4.3 |
| A26 | §2f `(D-a)` | Population scope is wrong and the fix is free. **Three** populations reach the binder-miss branch, not one: **lapsed** (ran, scaled to zero, TA state deleted after `2*DefaultObservationMaxAge` — TA's own comment concedes *"degrades to the never-seen case"*), **cold-but-priceable** (sibling/deployment estimate), **cold** (`satReasonNoData`). Saturation tiers 1–2 return stored `EffectiveCapacity` *authoritatively* when `LearnedFrom == learnedFromLive`, so a **measured** PRC often exists for exactly that variant. The discriminator is already at the write site — the identity carrier is located **by name with no `Enabled`/`Live` check**, so `a.Reason` is readable in every configuration — and the merge **discards** it (`out.Reason` assigned only inside the `bByName` hit branch). Keep the two narrowings **visibly distinct**: *"admit only when saturation votes"* is dead by reachability (sat's voting predicate is its binding predicate); *"admit only when the carrier has no record"* survives. Splits `(D-a)` into population scope and a separate `N8`/`VG-up` question about sizing from a stale carrier — deferring `(D-a)` does **not** dispose of the latter | designer §1/§2, endorsing + refining review F44 |

| A27 | §verification `:2033-2040` | The C6c gate **fails by design and will trip C9**. It requires that after `grep -rn "ceil(" internal/engines/pipeline/`, *"`fairShareCap`'s must be **gone**, replaced by the whole-replica `floor` fill"* — but `fairShareCap` no longer exists under that name (it is `replicasToCover`) and the tree ships `ceil` there deliberately, with a written rationale. Re-express against the new name whichever way the rounding question is decided. **Note what survives intact:** the gate's own one-sentence rule at `:2039-2040` — *"round up when asking how many replicas a demand needs, round down when asking how many replicas a budget can afford"* — is exactly the two-directional policy the code implements at `:697-700`. So the gate is not wrong in principle, only in its `fairShareCap` clause and its name | designer §1.3, verified |
| A28 | §4a — **corrects B3, does not close it** | Review Finding 33's four cited lines **are** fixed (`4fb49ac6`, *"drop plans-branch paths from shipped comments"*), and a `plan__|review__|sync__` grep across `internal/` is clean at tip. But the designer's conclusion *"Finding 33 is CLOSED — do not route it"* rests on that narrow expression. A broader one finds **two surviving violations, both PR-2's own** (absent at base `075a208e` **and** on `upstream/main`): `analyzer_helpers.go:642` cites **`combined-analyzer-optimizer-design.md § invariants #7`** — a direct plans-branch document reference, the exact class §4a names — added by `8eb6ee2d`/`b106b929`; and `analyzer_helpers.go:88` reads *"Type-1 owner's, not this file's"*, a taxonomy/role token, added by **`b6bb525c`** — the current tip, i.e. **after** the cleanup commit. So the in-tree burden is **not** monotonically self-correcting as claimed; C11 re-added one. This is the fourth mutually-inconsistent §4a count from three sessions, and all four expressions missed both of these lines — which is the argument for A24's "pin one expression" being a plan step, not a preference. (Separately: `internal/collector/locator/locator.go:4` cites `docs/superpowers/specs/…-design.md` — **inherited**, not PR-2's; belongs to the `main`-side backlog in `governance-follow-ups.md`) | my grep + provenance check; supersedes the designer's §3 closure |

| A29 | §C9 test row | **Finding 49** — two one-line §4a token edits, both verified present at `79a590d6`: `k_sat_test.go:163` *"Pre-C10 this priced at k = 0.85"* → "Before this change" (identical meaning); and `rescale_test.go:186` *"The from-zero admission ceiling (C11) is what stops…"* → drop "(C11)", the sentence already names the mechanism. Unlike the commit-message instances these are ordinary edits, not rewords — which is the whole argument for routing them **into C9 rather than after it** | reviewer Finding 49 |
| A30 | §C9 test row | **Finding 50** — `rescale_test.go:239` `max := 3` and `:248` `max := 8` shadow the Go builtin. Verified: these are the **only two** `max :=` declarations in `internal/**` + `cmd/**` at this tip, and the two sites ev-shindin objected to in the #1246 review are **gone** (`analyzer_helpers.go:977` now uses the builtin correctly), so PR-2 is the sole place reintroducing a pattern the codebase has moved off. **`make lint` cannot catch this** — gocritic's builtin-shadow check is off by default, so the coder's green gates are not evidence against it. `maxRep` costs nothing and no competing local idiom exists. A convention regression against a maintainer's stated objection, not a gate failure | reviewer Finding 50 |
| A31 | §C9 dev-guide row | C9 must **not** describe the from-zero ceiling as an active guard. Per Finding 46 nothing writes the tag and `(D-a)` is deferred, so the ceiling is **dormant in production**; a dev-guide sentence implying it currently bounds anything would be wrong on the merged branch. **Correction to the same handoff's §5:** its first item — *"`analyzer_helpers.go:213-216`'s 'Not proactively selectable' comment is now false and needs correcting"* — is **already fixed and needs no C9 row**. The line reference drifted (the comment is at `:178`, C11 having inserted above it) and `b6bb525c` **rewrote that very comment**: the old *"genuine cold-starts fall to the reactive scale-from-zero engine"* is gone, replaced by *"its sizing must not be invented … Proactively admitting the zero-replica case … is deferred; see `ReasonFromZeroAdmission`"*. That text is accurate **and** consistent with Finding 46 — the finding was carried forward without re-verifying against the tip the handoff itself declares it reviewed | reviewer §5 + my verification at `79a590d6` |

| A32 | §4a — **strengthens A24b; the sweep is not mechanical** | **Finding 51** (`analyzer_helpers.go:216-218`) establishes a principle that changes what a §4a step must *say*: the tokens are **load-bearing markers of "written before X landed"**, so some of the 54 locations are stale in **content**, not merely in vocabulary. Its instance: *"once **PR-2** admits multiple non-saturation voters, a later qualifying entry does not overwrite the earlier one"* — future-tense about a condition `votingResults` (`:315-323`) **already satisfies on this branch**. Strip the token mechanically and you get *"once this change admits…"*, which is §4a-clean **and still false** — and a clean sweep is the thing nobody re-reads afterwards. So the plan's §4a step must direct a **prose rewrite where tense or premise moved**, not a token substitution, and must say the residual risk at such sites is *higher* than at the other 46. Reviewer's own framing, and it is right | reviewer Finding 51 |
| A33 | §4a — the tension that will let A28 through | The coder's C9e is scoped *"47 of 54; the 7 inherited at `075a208e` out of scope"*, and the reviewer **endorses that as matching its ledger exactly** (54 − 7 = 47) while, in the same document, listing *"the path/filename class"* among the items that **"remain pending"** for that commit. Both statements are its own and they cannot both be complete: 54 is token-class only. That is the precise mechanism by which A28's two sites — `analyzer_helpers.go:642` (design-doc **filename**) and `:88` (*"Type-1 owner's"*) — pass through a sweep that is simultaneously certified exhaustive. **Re-verified: both still live at `757fc6f5`.** Scoping the sweep to the PR-2 delta is correct on its merits (the 7 inherited belong to the `main`-side governance backlog) — the defect is the *class* boundary, not the delta boundary. The plan step must therefore name **three** classes: tokens (47), plans-branch paths/filenames, and taxonomy prose (`Type-1`, `Type 3`) | my re-verification at `757fc6f5`, against the reviewer's §C9-gap + ledger-delta sections |
| A34 | §C9 — supersedes A31's first clause only | Distinguish two **adjacent but different** comments, because the reviewer's handoff and its review doc point at nearly the same lines for different things. `:213-216` *"Not proactively selectable"* → **already fixed**, no row needed (C11 rewrote it; it now lives at `:178` and is accurate — A31 stands). `:216-218` the **N2 tie-break** comment → **Finding 51, genuinely open** (A32). A31's correction must not be read as dismissing Finding 51. **Now moot as a disagreement — the reviewer has retracted it itself** (C9b pre-registration **P2**, retracting its own C11 checklist items 7 and 13): those items assumed `(D-a)` would ship, and with it deferred the *"not proactively selectable"* claim is **true as written**, so *"correcting" it would replace a true statement with a false one*. It pre-commits to scoring any regression there as **its own** error, not the coder's. So A31/A34 reduce to bookkeeping: apply them as a note, not as a counter-argument | my verification, corroborated by the reviewer's own P2 retraction |
| A45 | §C9 goldens-endgame row + §12 ledger | **C9d has landed and it is plan-mandated, correctly shaped, and consistent with the constraint Dean named as most important — record it as done, not pending.** Tip moved `2ae440e3` → `209e148f` (C9c) → **`4e369f10`** (C9d, *"remove the sat-only characterization goldens, scenario by scenario"*, 1 file, +17/−352). The plan already mandates it and names Dean as the source: `:1450` *"Endgame for those sat-only goldens — RELAX / REMOVE (Dean, 2026-08-06) → C9"*, `:1453` *"as an **explicit commit** (not an implicit drop) — do not leave them silently"*, precondition at `:290` *"once the multi-vote goldens cover the single-vote sub-case."* All three are satisfied, verified by me: the commit writes the A→M mapping out as a table rather than asserting it, and the mirror structure is **one-to-one** in the replacement file's own headers — `M1 (mirrors A1)` aggregated scale-up · `M2/A2` scale-down + veto · `M3/A3` idle no-churn · `M4/A4` cost tie-break · `M5/B1` disaggregated paired scale-up · `M6/B2` role-scoped veto · `M7/C1` namespace quota, GreedyByScore-only — under a `Describe` whose title is *"Multi-vote optimizer goldens — [sat] / [TA] / [sat, TA]"*, so the **`[sat]`-only ballot shape survives as a table entry in each**. That is the mechanism that keeps sat-only behavior pinned, which is the standing *"most important point is not to break current behavior when sat only"* — the coverage moved to a strictly larger suite rather than being dropped. Spec count 394 → 386 (the eight removed), eighth spec handled as **subsumption not identity** and said so. **Boundary of my check, state it rather than imply completeness:** I verified the mandate, the mapping's one-to-one structure and the `[sat]` shape's presence; I did **not** re-transcribe the per-entry literals behind the commit's *"identical is meant literally"* claim. That is mechanically checkable and worth one pass before push — it is the single load-bearing claim of the commit. **Two consequences:** (1) CURRENT.md's *"no golden moved"* framing is now definitively superseded — PR-2 removes 352 lines of the very file #1516's squash landed, **by design**; (2) it sharpens the #1513-close call rather than complicating it — closing a no-op PR whose content PR-2 then supersedes is coherent, but the close note should not claim the goldens are a durable gate | coder commit `4e369f10`; mandate and mirror structure verified by me against plan + object |
| A44 | §4a — supersedes A10's number, does **not** replace its metric | **Finding 59's recount is right in its delta and off by one in its total, and — more important — it measures a *different thing* than A10 and the plan text, so the three numbers must never be reconciled into one.** Verified by me at the current tip: `T1-ols` is **36 occurrences across 12 files**, not 35/11, and the count is **identical at `209e148f` and `4e369f10`** so tip drift does not explain the gap. The missed file is `internal/engines/pipeline/optimizer_invariant7_test.go` (1 hit) — created by `209e148f`, the very commit Finding 59 correctly attributes 13 additions to (`^+.*T1-ols` = **13**, exact). Trajectory for this token alone: **19/9** at `d9f3b97e` → **36/12** now. Distribution is concentrated: `analyzer_helpers_test.go` 12 and `optimizer_multivote_characterization_test.go` 12 are **24 of the 36**; only **two** are non-test `.go` (`domain/analyzer.go`, `throughput/constants.go`) plus one doc (`docs/developer-guide/cycle-log.md`). **The three-metric distinction, which is the actual content of this row:** plan `:1647`'s **32** = all token families, earlier tip; **A10's ~49** = all token families, later tip; **Finding 59's 35/36** = the `T1-ols` family only. So Finding 59 **does not revise A10** — it is a narrower cut, and A10's "production `.go` went 0 → 19" refers to the broad family, consistent with `T1-ols` contributing only 2 production hits. A broad-family re-count at tip lands far above all three, but I am **not** entering a number for it: my regex over `T1-ols|FZ-admission|W1-5|N1-9|U1-5|D1-2|V6|F1-13` returns 75/26 and I have **not** eliminated false positives (`F1`, `D1`, `N5` &c. have innocent readings), so the honest record is "materially higher than 49, precision unestablished." Any count written into the plan must carry **both** its token set and its tip. **⚠ RETRACTED IN FRAMING 2026-08-08 — the count is right and it governs nothing.** `T1-ols` is **`itlReasonT1OLS`**, declared at `throughput/constants.go:129` (*"tier-1: OLS fit from live observations"*) and consumed as a published `Reason` string — corroborated at `domain/analyzer.go:150` (*"Throughput uses `T1-ols`"*) and `docs/developer-guide/cycle-log.md:116`. It is a **shipped product identifier**, not a plans-branch token, so all 36 occurrences are a §4a **carve-out** and none is a violation. Proof from the tree rather than from reading: C9e — the commit whose entire purpose is making shipped references resolvable from `main` — left **all 36 untouched**, and the coder's close-out states *"confirmed real and left alone … Stripping any would break code."* I reached this by asking why C9e skipped them instead of assuming the sweep was incomplete. So this row survives **only** as (a) the three-metric caution, which stands, and (b) evidence for B12 that counts are the wrong instrument. Do **not** carry 36/12 into the plan as a §4a figure | reviewer Finding 59; count corrected, then its framing retracted, by me |
| A43 | §C9 sweep row — Finding 58 is a **class**, not a site | **Verified exact, and the generalization matters more than the site.** `analyzer_helpers.go:659` reads `// combined-analyzer-optimizer-design.md § invariants #7.` — confirmed character-for-character at the stated line, in a shipped production doc comment on `refreshAnchorSizing`, new in PR-2. It violates §4a on **both** listed counts at once: a pointer to a plans-branch document *and* a section identifier (`§ invariants #7`). **Why the reviewer is right that it is outside the pattern:** it contains no token, so every token-family grep — `4fb49ac6`'s, the reviewer's own enumeration, and A44's 36-hit `T1-ols` count — misses it by construction. The sweep therefore needs **two shapes**, not one: (i) token identifiers, (ii) plans-doc **filename/path** citations. **I swept shape (ii) myself so the row can state coverage rather than suspicion:** `git grep -n 'combined-analyzer-optimizer-design'` returns **exactly one** hit — this site — and `git grep -nE 'planning/|-plan\.md|-review\.md'` returns exactly one other, `docs/developer-guide/throughput-analyzer.md:714` citing `plans/planning/TA-Plan.md` + `TA-PR4-plan.md`, which is **pre-existing on `main`** and already tracked as a main-side §4a location in `governance-follow-ups.md` — **not PR-2's to fix**, and worth saying so explicitly so the sweep does not silently widen into inherited debt. So shape (ii) on PR-2 is a **single-site** fix. **⚠ COMPLETENESS CLAIM NARROWED 2026-08-08 — the conclusion holds, "fully enumerated" does not.** My pattern (`planning/|-plan\.md|-review\.md|combined-analyzer-optimizer-design`) misses the bare **`TA-*.md` design-doc family**, and there is a live instance: `throughput/constants.go:85` reads *"This approximation only holds in the decode-dominated regime (N_pre ≈ 1, **TA-supply.md §3.1**)"* — a plans-branch Type-1 citation **plus** a section identifier, in a shipped production comment, matching none of my four patterns. It is **inherited** (present at `075a208e`, one of the 8), so PR-2's single-site conclusion survives intact; what does not survive is the claim that I swept the class. **This is the strongest evidence for B12**: I wrote a pattern, certified it exhaustive, and it was under-inclusive on its first widening — the same defect A33 identifies in the reviewer's ledger, committed by me one round later. Any plan-side pattern must enumerate the doc families (`combined-analyzer-optimizer-design`, `TA-*.md`, `*-plan.md`, `*-review.md`, `planning/`) and be stated as a gate, not a count. **Fix status: LANDED, and in the right shape** — C9e (`a9afb740`) did not delete `:659`'s citation, it **repointed** it to a doc that ships: *"see `docs/developer-guide/multi-analyzer-pipeline.md`, 'Scale-up path', under 'Per-iteration anchor refresh'"* — preserving the cross-reference's value for a `main` reader instead of stripping it, which is the pattern the remaining sites should follow | reviewer Finding 58; line verified, sweep run and then its coverage claim narrowed, by me |
| A41 | §2f + §4 rebase step + §12 ledger | **Partial scale-from-zero, per config — and `[sat]` default is verified SAFE, which is the constraint Dean named as most important.** Designer's traced verdict, three supporting legs re-verified by me at `2ae440e3`: `[sat]` **WORKS** both non-P/D and P/D; `[sat, TA]` works non-P/D (sat's vote carries, because `prcForVariant` returns 0 and `votesFromPickerState` **abstains** rather than voting zero, and scale-up is MAX over non-abstainers) but is **BROKEN** in P/D; `[TA]`-only works only if TA measured the variant before. **Why `[sat]` is immune, mechanically** — (i) a genuine **three-tier** ladder at `saturation_v2/analyzer.go:411-431`: live-over-ready → own stored record → `lookupCompatibleCapacity` cross-sibling; (ii) tier 2 is **spec-derived, no pods required** — `capacity_store.go:88` `LoadFromScaleTarget` takes a `ScaleTargetAccessor`, called per-VA at `engines/saturation/engine_v2.go:50`, and its comment states the intent outright (*"brand-new variants with no live data or compatible siblings can still be considered for scale-up"*); a refinement the designer did not state and that strengthens it — `:85-86` says spec-derived data *"does NOT overwrite an existing live record"*, so the tiers are ordered by design, not by accident. **⚠ Write the cell with its preconditions attached — a bare "WORKS" is the same defect one layer down.** Tier 2 is pod-independent but **not unconditional**, both verified by me: (1) the call is *gated*, not unconditional — `engine_v2.go:42-46` does `if scaleTarget == nil { …DEBUG…; continue }`, so a scale target that does not resolve yields no pre-population and says so **only at DEBUG**; (2) `capacity_store.go:126-128` sets `EffectiveCapacity = EffectiveMaxBatchedTokens` only when that value is `> 0`, so a spec exposing no budget the parser recognises leaves it at 0, tier 2's own `rec.EffectiveCapacity > 0` gate fails, and the ladder falls through tier 3 to `satReasonNoData`. Both normally hold in the posed scenario (a zero-replica variant still has its Deployment/LWS object), **so the verdict stands** — but the accurate grounds are *"works wherever saturation can price the cold variant from spec or from a compatible sibling,"* not *"works in every config where saturation votes."* State it precisely because the residue is **silent at DEBUG** — the same failure character as the `[TA]`-only cell the deferral is about, so an unqualified "WORKS" invites a later reader to read the `[sat]` column as a guarantee; (iii) sat sets `Role: vs.Role` **unconditionally** at `analyzer.go:445`, inside the single `append`, so it can never manufacture a phantom bucket. Residual under `[sat]` is only `N5` (`Cost = 0` mis-**ranks** a zero-replica variant) — a ranking defect, not a failure to come up. **Consequence for §12:** the ledger must record that the P/D break is a `[sat, TA]`-and-`[TA]`-only condition, so it cannot be described as a general regression | designer's per-config verdict handoff §1; all three legs verified by me |
| A42 | §4 rebase step — two routes, not one | **There are two independent mechanisms that stall an entire model, and only one is live on this branch. They must not be conflated in the plan text.** Both were verified by me and both terminate in the same allocation loop. **Route 1 — phantom role, LIVE today, fixed by the rebase:** blank `Role` → canonicalized to `RoleBoth` → `initRoleState` unions `"both"` into `roles` → `pick("both")` finds no anchor variant → `allPicked = false` at `analyzer_helpers.go:951` → `break` at `:958`. This fires on the **first** iteration, before any commit, so the model gets **zero** decisions including the healthy co-tenant. Designer's refs are exact here — I checked. **Route 2 — anchor-only sentinel, hypothetical, the reason `(D-a)` must not ship anchor-side:** sentinel on the anchor but not the ballot → `demand <= 0` → `utilByRole = 1.0` at `:969-971`, which **defeats** the `deltaUtil <= 0` guard at `:982` built for exactly this → `k` computed only when `demand > 0` ⇒ `!anyPositive` ⇒ `break` at `:1001`. Same outcome, different door, different disposition: Route 1 is a bug awaiting the rebase, Route 2 is an argument about a deferral. Writing them as one item would make the rebase look like it also closes `(D-a)`, which it does not | designer §1.3 (Route 1, refs exact); Route 2 is my own earlier trace |
| A39 | §2d.5 row 6 + C6c row + §4 reword window | **The `ceil`/`floor` divergence (`T1-1`) is undiscoverable from the branch, and that is the defect — it holds whichever direction Dean picks.** Type 1 `:1163-1168` mandates the change in four parts: direction, reason, *"a fixture that lands mid-replica, or it will not be observed"*, and *"Flag it in the commit message."* Parts 3 and 4 are **formally satisfied and substantively inverted**, both verified by me at `2ae440e3`: the mandated mid-replica fixture exists — `greedy_score_optimizer_test.go` `It("rounds the entitlement up to a whole replica and the pool down")`, a 5-GPU entitlement at 2 GPUs/replica, i.e. exactly mid-replica — and asserts `capN == 3` where the authority gives `floor(2.5) = 2`, with an in-code rationale (*"The half is still owed, and a replica is the smallest thing that can be handed over"*). `34b18bc5`'s message argues round-up is *"the rule"*. The strongest site is `greedy_score_optimizer.go:697-700`: *"The two terms round in opposite directions **on purpose**"* — an argued design statement contradicting the frozen authority. Net: a reviewer reading only this branch concludes the direction was chosen deliberately and correctly. **Type-3 consequence:** row 6, the C6c row and the value-neutrality row each transcribe the mandate, so each needs the divergence named as a divergence, not just the direction restated. **Type-4 gap (C9 is the host):** `grep -in 'fairShareCap\|replicasToCover' -- docs/` returns **zero** hits — no dev-guide text states either direction, on a change the authority itself calls non-value-neutral | reviewer's fairShareCap-fork handoff §1–§2, §4; all four parts re-verified by me against the object |
| A40 | §4 reword window — second independent reason | **The §4a reword decision now has a second, unrelated justification, which changes the 9-vs-16 calculus already queued for Dean.** `34b18bc5`'s message body is one of eight `ceil`-endorsing sites and the only one with a clock: rewritable while the branch is unpushed, permanent once PR-2 opens. So the window buys a correct commit-message record of the rounding fork *in addition to* the plans-branch-token cleanup — two reasons for one action. **A grep-driven fix-up cannot substitute, and I verified why it is actively dangerous, not merely incomplete:** `git grep -in ceil` over the two fork files returns **11** hits, of which **one** (`:837`, the actual `math.Ceil`) is the fork; the rest are the demand→replica `ceil` the authority mandates and the reviewer explicitly ring-fences (`ceil(20000/10000)` &c.), plus `ceiling` in the maxReplicas sense. **And `:453-456` is a token trap in both directions** — it contains the word *"floor"* (*"One replica's worth of demand is the floor"*) while endorsing round-up, so a `grep -i floor` compliance check reads it as satisfying the mandate and a `grep -i ceil` misses it entirely. Any sweep here must be a prose review, not a token pass — same conclusion as the §4a carve-out item, reached independently | reviewer §4; hit counts and the two-way trap verified by me |
| A38 | §4 rebase step — A36 addendum | The phantom `"both"` bucket has a **second, separable effect** that A36 and both source handoffs missed: **decode demand is understated by exactly 2×.** Verified at `2ae440e3` — `distributeDemandByRole:918-941` builds its role set with `RolePrefill` **excluded**, maps blank → `RoleBoth`, then assigns every role the *same* `share := demand / float64(len(roles))`. Correct P/D gives `roles = {decode}`, `len == 1`, decode gets the full model decode demand; the phantom gives `{decode, both}`, `len == 2`, decode gets **half**. That share flows `aggregateRoleCapacities` → `RoleCapacities[decode].TotalDemand` → the engine post-step that writes per-role RC/SC. **Why it is separable from the break:** RC/SC is computed and published on a path that does not depend on the pick succeeding, so the `break` suppresses *decisions* while telemetry keeps reporting a **halved decode requirement** — which reads as a healthy under-subscribed role, not a stalled one. Same signature class as the `OptimizationReady=True`-with-no-event bug `a38d7b73` also fixes: the cluster stops acting and the telemetry does not say so. It also **survives break→skip**, which would leave the dilution intact while making the symptom less visible. Two consequences: (1) a **fourth argument for shape 1** — `st.role` fixes both effects at the derivation, a one-liner-plus-nothing does not fix the telemetry story, and severity language must name both; (2) **a live hazard for C9c, in flight right now** — if the multi-vote goldens use a P/D fixture with any zero-replica variant, the golden **bakes the halved decode demand into a characterization test**, which is worse than the break because the break is obvious and a wrong frozen number is not. **Attribution (keep this framing):** the construction is byte-identical at `075a208e` and PR-2's 41/12 delta on that file contains **zero** `Role` hunks (verified) — inherited from PR-1, absent only because PR-2 is stacked on the pre-merge tip, **not a PR-2 regression**. Also narrows the designer's §3: `saturation_v2/analyzer.go:136` does populate `RoleCapacities`, so its `else`-branch generalization is a correct conditional over a narrow live surface, and PR-2 adds no `RoleCapacities: nil` producer — **PR-2 does not widen the hole** | reviewer's second-effect handoff §1–§3, every claim re-verified by me; upstream's own replacement comment names both effects |
| A37 | §0 state-of-record + §2e | Three corrections to my own tracking, all verified by `rev-list --count` rather than subject-matching (the shortcut that produced drift in three documents). **`b6bb525c..HEAD` is 4 commits, not 1** — so **C10 has LANDED** as `1a50b418` *"throughput: read k_sat from config instead of hard-coding it"*: §2e stops being pending work and becomes shipped, and any text saying the coder is "now on C10" is wrong — it is past it. Tip is **`2ae440e3`**, with one untracked file (`optimizer_invariant7_test.go`), so C9's test half is mid-flight. **T1-1 (`ceil` vs `floor`) is withdrawn as blocking and as a C6c scheduling constraint** — C6c landed as `34b18bc5` before the instruction was written, and `replicasToCover:834-838`'s `math.Ceil` (the only `Ceil`/`Floor` in non-test `greedy_score_optimizer.go`) carries a written rationale at `:826-836`. It reduces to a **doc-vs-code divergence** — frozen Type 1 mandates `floor`, tree ships `ceil` with justification — still Dean's call, with the safe side already compiled. My narrowing of the non-termination mechanism is adopted by the designer: the hang needs `allocateForModel` to return **true while allocating nothing** (since `!allocated` sets `w.remaining = -1` and terminates), so it is conditional, **not a property of `floor`** | designer's withdrawal handoff §2/§4, re-verified by me |
| A36 | §4 rebase step (**highest-value row in this batch**) | The rebase step must name the four behaviors `a38d7b73` brings and require a post-rebase message-vs-diff check for each, because **PR-2's branch is missing all of them** — verified: `git merge-base --is-ancestor a38d7b73 HEAD` → **absent**, since PR-2 is stacked on PR-1's *pre-merge* tip. One of the four is a **live P/D break already on this branch**, sentinel-independent: the scale-from-zero complement at `throughput/analyzer.go:435-439` emits `VariantCapacity` with **no `Role`** (confirmed at `2ae440e3`); `distributeDemandByRole`/`aggregateRoleCapacities` consume that same slice *before any anchor merge* and canonicalize blank → `RoleBoth`; on a disaggregated model `len(byRole) == 3` so the only-`both` nil guard misses; `initRoleState` unions ballot role keys; `pick("both")` finds no anchor variant (anchor roles come from the saturation (a)-carrier) ⇒ `allPicked = false` ⇒ **`break` on the first iteration ⇒ zero scale-up decisions for the whole model** whenever a previously-measured variant sits at zero. TA need not *bind* — enabled + live is enough, since the union is over the voting set. **Correction to the handoff proposing it:** the fix is **`Role: st.role`** (persisted state, hunk `@@ -408,6 +415,7 @@`), *not* `Role: vs.Role` as written — they are kept in sync by `:253` so behavior likely matches, but "identical text to `main`, a clean no-op at rebase time" is then **false**, which is the entire safety argument for the one-line-now shape. **My routing: shape 1 (rebase), not the one-liner** — the rebase is due anyway, the other three fixes are real and two are operator-visible (`OptimizationRefused` + Warning event; the `wva_desired_replicas=0` KEDA-reads-as-scale-to-zero rescue), and a lone one-liner leaves the branch fixing the *quiet* bug while still carrying the two *loud* ones. Sequencing also argues for it: injecting a source edit mid-C9c muddies the per-commit golden attribution the C6c-first ordering exists to protect. Also record the **CURRENT.md reading trap**: "Finding 12 is FIXED, not deferred" is true of `main` and **false of PR-2's branch** | designer's P/D handoff §1/§2, every step verified by me at `2ae440e3`; `a38d7b73` message derives the same chain independently |
| A35 | §2f `(D-a)` + §12 deletion/deferral ledger | Rewrite the `(D-a)` deferral as a **§4b DEFERRED classification with a mechanism clause**, not a one-line TODO. Three required parts, because a future implementer reading only the frozen Type 1 builds the regression: (1) **what it would do** — proactively admit a never-measured zero-replica variant so the cost/fair-share ranking can pick it; (2) **why it is not shipping** — the frozen mechanism is insufficient, not merely unfinished, and today's behavior is safe without it (anchor PRC 0 ⇒ `cost_aware_optimizer.go:100` passes the variant over ⇒ co-tenant picked normally); (3) **the mechanism the follow-up must carry** — the sentinel is written on **the binding analyzer's own ballot entry**, and the (b) merge carries it to the anchor as a consequence; an anchor-only write ships a regression that takes the **co-tenant's** scale-up down with it via the `demand = 0` → `utilByRole = 1.0` → `!anyPositive` → `break` chain, and in P/D silently commits prefill without decode. This is the one part of the designer's ask that lands in a doc **I own**, and it is independent of Dean's scope call — it is required whether `(D-a)` ships in PR-2 or not (if it ships, the same clause is the spec; if it defers, it is the deferral record). **REVISED 2026-08-08 — part (2) above is retracted at source and must be rewritten.** The designer withdrew *"nothing is broken today"* after Dean named a counter-scenario, and traced it per config: partial scale-from-zero breaks in **two of three** configs. So the deferral text **must not** say "no live gap" — that is now a known-false justification, and it is the sentence that would make a future implementer treat the gap as absent rather than owned. Corrected five-part content, per the designer's §5 and adopted by me: **Gap** — `[TA]`-only with a variant TA has never measured, or whose per-variant state lapsed past 2× observation max age. **Failure mode** — **silent**: `costGreedyRolePick:100` skips it, no event, no log, no condition. **Not covered by** — the reactive `scalefromzero` engine (it fires only when the *whole model* is cold, so a model-level `minReplicas ≥ 1` means it never fires), nor by saturation's ladder (`satEnabled` false ⇒ the per-variant (b)-fallback is off — this is `N1`); and, per the reviewer's refinement which I verified, sat's ladder also yields nothing when **the scale target does not resolve** (gated at `engine_v2.go:42-46`, logged at DEBUG only) or when **the spec exposes no token budget the parser recognises** (`capacity_store.go:126-128`) — pod-independent, but not unconditional. **Mechanism when taken** — ballot-side `Reason`-tagged `PRC = 1` plus C11's landed one-replica target ceiling, which turns the arbitrary `1` into a one-replica *probe* that real measurement replaces next cycle; that framing is also why it does not violate `N8` (a declared minimum, not a borrowed measurement). **Owner** — the F1 saturation-disable redesign, **not** PR-2. Part (3)'s ballot-vs-anchor clause is unaffected and still required | designer's `(D-a)` handoff § recommended fix; **revised** by its per-config verdict handoff §5, whose three supporting legs I verified |
| A46 | §0 state-of-record + §4a ledger — **supersedes A9 and A10's numbers** | **The authoritative §4a scope is 54 PR-2-introduced (all fixed) + 8 inherited = 62, and the reword window is 21 of 24 commits, not 13.** Both figures are the coder's close-out ledger and both supersede mine: A10's ~49 was a mid-flight count on a growing branch, and A9's 13 predates C9a–C9e. Plan status line *"47 of 54 … 7 inherited"* → **"54 of 62, 8 inherited"**. The 8th inherited site is `throughput/constants.go:85` (`TA-supply.md §3.1`) — see A47, and note it is the site that broke A43's pattern. **Why the reword number nearly doubled and why that is not drift:** every commit added since A9 was written extends the count — which is precisely B12's argument that a plan-side integer is stale on the next commit. **Record the count with its tip attached or not at all.** **⚠ Two figures corrected 2026-08-08, both verified by me, and one of them was mine from an hour earlier:** (1) the denominator is **25, not 24** — `rev-list --count 075a208e..HEAD` = **25**, so the window is **21 of 25**; (2) **my "C9e is clean" was wrong.** `a9afb740`'s own body carries **15** distinct plans-branch tokens — `N2 N3 N7 N8 W1 W4 U2 T1.4 C2 C7 C9e C10 C11 C6e D-b` — as objects of description. That is worth recording beyond the arithmetic: **the one commit whose stated purpose is *"make every reference in shipped comments resolvable from `main`"* has the least resolvable message on the branch.** It is A32's lesson recurring one level up — the sweep fixed the artifact class it was scoped to and reproduced the defect in its own commit message, which no grep over `internal/` would ever see. **Also reconciles the figures rather than overruling them:** 54-vs-49 were measured at different tips and **both hold at theirs**, so A10 was not wrong, only earlier; and the 7-vs-8 inherited gap was the reviewer's own filter excluding test descriptions. All 8 inherited sites are present at `075a208e` **and in `upstream/main`**, which is what makes them the main-side backlog's and not PR-2's. The window is still Dean's call and still time-boxed to PR-2 opening; the cost is **21 now vs 25 later** — a narrower gap than the 9-vs-16 previously queued, which weakens rather than strengthens the case for doing it now | coder's close-out ledger §3 + reviewer's corrections; commit count, C9e's tokens and the 8th site all verified by me |
| A47 | §12 ledger — **out of PR-2 scope; route to the main-side backlog** | **Two inherited class-1 sites, both verified live at tip, neither PR-2's to fix — record them so they are not swept into C9 and not lost either.** (1) `throughput/constants.go:85` — *"the decode-dominated regime (N_pre ≈ 1, **TA-supply.md §3.1**)"*: a plans-branch Type-1 doc **plus** a section identifier in a shipped production comment. Present at `075a208e`, so inherited. Belongs with the existing main-side §4a locations in `governance-follow-ups.md`. (2) `cmd/main.go:165-169` — a **dead public link in shipped code**, and a different class worth separating: the comment sends the reader to `https://github.com/llm-d/…/blob/main/docs/user-guide/configuration.md`, and I verified that file **does not exist** — `git ls-files 'docs/user-guide/'` returns exactly two entries, `monitoring.md` and `sglang-backend.md`. So this is not a §4a violation at all (nothing plans-branch about it); it is a 404 shipped to users, pointing at `main`. Arguably higher-severity than the §4a items precisely because it *looks* resolvable. (3) **A third, and this one is PR-2-adjacent enough to be worth C9's attention:** `docs/developer-guide/throughput-analyzer.md:609` reads ``See [`saturation-scaling-config.md`](../saturation-scaling-config.md)`` — from `docs/developer-guide/` that `../` resolves to `docs/saturation-scaling-config.md`, which **does not exist**; the real file is `docs/developer-guide/saturation-scaling-config.md`. Verified both halves via `ls-files`. So the fix is deleting two characters, and unlike (1) and (2) it sits in a **Type-4 doc C9 is already editing** — a rendered-broken link in the very dev-guide the sweep is repointing comments *into*. **Pattern across all three:** the §4a sweep is about references that are unresolvable *because they point off-repo*; these are unresolvable *while pointing in-repo*, and no token grep finds either class. A link-resolution pass is a different instrument from a token grep — the same "right tool per class" point as B12 and A40. **(1) and (2) are file-an-issue items at Dean's direction, not C9 rows** — widening C9 into inherited debt is the failure mode A43 already flagged — but **(3) I would fold into C9** as a two-character fix in a file it already touches | (1)–(3) all verified by me at tip; (1) from the coder's ledger, (3) from the reviewer's correction, (2) surfaced by me |
| A48 | §4 ship-gate row — **close it as assigned and landed** | The coder flagged an **unassigned PR-level ship gate**: §4 `L1440-1449`'s invariant-7 check (a *direct* test that a one-analyzer `[sat]` ballot leaves the anchor field-for-field equal to the saturation entry before/during/after allocation, **plus** a not-invoked assertion on the per-variant sizing refresh) was *"currently nobody's"*, and offered to take it in C9. **It landed.** Verified: `internal/engines/pipeline/optimizer_invariant7_test.go` is tracked at tip, **269 lines**. So the plan's §4 must attribute the gate to C9 rather than leaving it unowned — and this is the file whose omission caused Finding 59's off-by-one (A44), which is a small illustration of why an unassigned gate is dangerous bookkeeping even when the work gets done. Note the gate tests exactly Dean's most-important-point in its strongest form: `[sat]`-only leaves the anchor untouched | coder's close-out § also-worth-recording; existence and size verified by me |
| A49 | §6 grep bullet — the C6c criterion **cannot fire** | **§6's `ceil(` grep is case-sensitive, so the C6c compliance check is vacuously green.** Go spells it `math.Ceil(`. **⚠ Two self-corrections, 2026-08-08 — my own mechanism was wrong twice, and the true version is a *stronger* form of the same point.** (i) The case-sensitive `ceil(` grep does **not** return zero hits — it returns **30**, every one of them in a `_test.go` file across nine files. That is worse than zero for the purpose, not better: 30 hits looks like a grep that is working, whereas zero at least looks suspicious enough to re-run. A compliance check that reports 30 irrelevant matches while structurally unable to reach any non-test site is the most deceptive of the three possible failures. (ii) The case-insensitive repair returns **four** non-test sites, not three — I had scoped to `internal/engines/pipeline/` and so missed **`queueingmodel/analyzer.go:379`**; across `internal/` the count is four. The three in-pipeline hits classify exactly as the coder said: `greedy_score_optimizer.go:837` (`replicasToCover`, the fair-share cap — the T1-1 fork itself) and `analyzer_helpers.go:629` + `rescale.go:598` (both converting a combined demand vote into a replica count — the *"a demand needs this many replicas"* case the authority says must **stay**). Line numbers drifted from the coder's report (`:520`→`:629`, `:585`→`:598`, `:619`→`:837`) but the classification is unchanged. **Fix: `grep -rni "ceil("`, and name all three expected hits with their dispositions inline** so a future runner can tell "compliant" from "pattern typo" — a grep that finds nothing is indistinguishable from a grep that passes, which is the same defect class as B12. **Also record: C11 has no §6 grep bullet at all**; the coder declined to invent the criterion, correctly | coder's close-out item 4; re-run and re-classified by me at tip |
| A50 | §2 #5 (i) (`L394-397`) — factually wrong about where the factor lives | **The plan says `GPUsPerReplica` *"lives on the same `VariantCapacity`"* and calls `gpusPerReplica(vc)`. Both are false, and the decided option-(a) signature therefore cannot reach the factor at all.** Confirmed from the shipped call site: `greedy_score_optimizer.go:690` reads `gpusPR := gpusPerReplicaFromState(stateMap, vc.VariantName)` — the only accessor, and it takes the **state map**, not the capacity. The coder extended the signature instead: `claimGPUs`/`fairShareValue` now also take `variants []domain.VariantCapacity, stateMap map[string]domain.VariantReplicaState` — *"same shape as the decision, one more parameter than it anticipated."* **That is the right call and needs no ruling** — the decision's *shape* survives; only my description of the data model was wrong. Correct the two sentences so the next reader does not re-derive the unreachable signature | coder's close-out item 2; accessor signature verified by me |
| A51 | §2 site (v) — **strike the inconsistent statement, do not leave it as apparent authority** | **The plan states the fallback rule two ways in two places, and site (v)'s version is wrong twice over.** It says to build the fallback from `combineVotes`, but (1) `votesFromPickerState` **already divides by PRC** to produce replica-space votes, so converting its output to GPUs divides by PRC a **second** time; and (2) `combineVotes` is **score-weighted**, which puts a ranking weight into the number that becomes the model's spend — exactly what **invariant 11** (*"priority orders, never scales"*) forbids, and the same invariant that settles Q4/unit-table row 5. The coder implemented the other, consistent statement: a plain **unweighted `max_i`**. **This is mine to fix and it is not optional** — an internally contradictory plan leaves the wrong branch citable as authority, which is the precise mechanism A32 describes for stale prose surviving a mechanical sweep. Strike site (v)'s `combineVotes` formulation; keep the unweighted `max_i` | coder's close-out item 3; invariant-11 conflict is the plan's own rule |
| A55 | §2d.5 row 6 + §6 grep bullet + C6c row — **the fork is re-measured at HEAD and resolves to `floor`; one residual sweep is larger than any instrument has reported** | **The refreshed number the fork was waiting on: 5 failures of 386 at HEAD under `floor`, worst delta −2, and zero collapse-or-eviction rows — against 9 of 334, worst −4, two collapse rows at C6c.** Verified by me at `a9afb740`: tree **clean** and `:837` back to `math.Ceil` (mutation reverted as the coder states, nothing committed, 25 commits on `075a208e`); all five named failure sites exist and read exactly as reported (`:175` and `:230` `Equal(4)`, `:1405`/`:1592`/`:1613` `Equal(3)`); and **both** C6c-era worst rows are present at HEAD — `optimizer_equivalence_test.go:135-137` (`ca["bv"]` = 6, the −4 under an unconstrained budget) and `greedy_score_optimizer_test.go:2148-2157` (the symmetric `team-a`/`team-b` row). **The coder's own care is worth mirroring rather than restating:** it flags that 9→5 is *not* like-for-like (334 → 386 specs; C9c added 24, C9d removed the sat-only goldens), declines to decompose the drop between the guard and the churn, and does not recommend `floor`. The like-for-like evidence it substitutes is the right one and is what actually decides the fork — the two worst rows still exist and now pass. **A detail that strengthens it beyond what the handoff claims:** the `bv` row's expectation does not depend on the rounding direction at all, because its own comment narrates `ceil(50000/10000) = 5` and that division is **exact**. A row whose arithmetic is rounding-invariant cannot fail *from* rounding — so its C6c 6→2 was necessarily the eviction path, and its green at HEAD is direct evidence the guard closed that path, not evidence about `floor`'s arithmetic. **One of the five is not a cost, and the coder is right about it for a reason it understates:** `:1405` asserts `capN == 3` for a 5-GPU entitlement and its comment *argues the round-up in prose* — but the same spec's second half (`:1409-1413`) asserts the **pool** case rounds down, so the spec exists specifically to document the entitlement-vs-pool **asymmetry**. If the fork resolves to `floor`, that spec does not merely flip an expectation: **its reason for existing dissolves**, both terms being floored. It should be rewritten as a both-floored statement or retired, and either way it is a specification change by design, not collateral. **Real price: four specs** — `:175`/`:230` (the design-doc walkthrough: starved model takes 2 not 4, priority-weighted split flattens) and `:1592`/`:1613` (C6e's one-entitlement-per-model specs) — every one 1–2 replicas short at a mid-replica boundary, which is precisely the plan's stated stop condition and the shape that was **not** met at C6c. **⚠ My A54 prediction was half wrong and I am recording which half.** I predicted "collapses among models already holding replicas, but fewer than 9." Fewer than 9 ✓ (5). **Collapses: zero — falsified.** My *code reading* stands (`allocated` is per-call progress, `firstDraw` is keyed on cumulative `targets` and so fires once per model per water-fill, not per iteration), but my *inference* that this leaves a materially-populated eviction path is unsupported by behavior. Review's parenthetical, which I called a conflation, lands closer to the observed truth than my correction did — though for a reason neither of us established: the post-first-draw eviction path is real in the code and simply is not reached by the measured scenarios. Reading a mechanism correctly and predicting its population are different acts, and I conflated them. **⚠ RESIDUAL — the largest single under-report of the whole §4a-adjacent pattern, and it is a *new shape*.** The coder says four endorsement sites plus prose must move together and that `greedy_score_optimizer.go` has eight of which only `:837` is reachable by `git grep -i ceil`. Measured: that grep returns **3** hits in the file, of which **2 are false positives** in the *cap* sense, not the round-up sense (`:714` "exhausted ceiling", `:847` "replica ceiling") — the same false-positive class as the `floor`-means-minimum 3:1 rate review reported. Round-up **endorsement prose** meanwhile spans **six distinct comment blocks** — `:454-456`, `:610`, `:698-699`, `:704`, `:727`, `:824-831` — **none containing the token `ceil`**, because Go spells it `math.Ceil` while English spells it *"rounds up"*, *"indivisible unit"*, *"whole replica"*. So the instrument reaches **1 of 7** sites and is 33% precise on what it does reach. **This is shape (iv): prose that endorses a behavior is invisible to a token grep of that behavior's Go spelling.** It is not a variant of the three §4a shapes already named — it is a fifth consecutive round in which an instrument certified exhaustive proved under-inclusive at its own boundary (A53's pattern, now with a fresh mechanism). Concretely: resolving the fork by flipping `:837` and sweeping grep-reachable sites would leave **six comment blocks actively arguing for the opposite of what the code does** — strictly worse than no comment, and exactly the transient state the coder's own uncommitted mutation exhibited (`math.Floor` under a `:825` doc comment reading *"rounding up"*). **So C6c's residual is a prose sweep of six blocks plus a rewrite-or-retire of `:1405`, and it must be specified by line, not by grep** | coder's HEAD re-measurement handoff; tree state, the five sites, both C6c-era rows, the rounding-invariance of `bv`, the `:1405` asymmetry structure, and the 1-of-7 grep reach all verified by me at `a9afb740` |
| A54 | §2d.5 row 6 + C6c row + B10/B5/B7 — **the fork's decision input is stale, and the mitigation is narrower than the reviewer states; suspend the recommendation** | **The reviewer is right that the 9-failure measurement predates its mitigation, and I have a correction that narrows the mitigation to roughly the population the failures came from — so neither side can predict the refreshed number and the fork should not be decided until it lands.** Reviewer's four-link chain verified by me against the **committed** object at `a9afb740`: `firstDraw` is **absent** at C6c `34b18bc5` (zero hits) and introduced by **C6e `784c2b5c`** (`log -S`, two commits later); `firstDraw := spentGPUs == 0` at `:660`; guard `if firstDraw && capN < 1 { capN = 1 }` at `:702`; `allocated := o.allocateForModel(…)` at `:299` with `if !allocated { w.remaining = -1 }` at `:301-302`; and `allocateForModel` returns **`w.remaining < oldRemaining`** (`:398`/`:532`) — *progress*, not pick-emptiness. All exact. Its asymmetry point is also right and is the good part: `replicasToCover` returns 0 only when `entitlementGPUs <= 0`, so under `ceil` the guard fires only on an exhausted balance, whereas under `floor` it fires whenever `0 < share < gpusPR` — the sub-replica entitlement that produced the collapse signature. C6e was written for the `ceil` world and is incidentally a `floor` mitigation. **⚠ My correction, and it is load-bearing: the guard fires once per model *ever*, not once per outer iteration.** `spentGPUs` is not a per-call counter — `:646-649` **recomputes it from `targets`**, the model's cumulative committed allocation, and `firstDraw := spentGPUs == 0` at `:660` is therefore true only while the model holds **zero** replicas across the entire water-fill. Once a model has *any* replica committed, `firstDraw` is false for the rest of the run. So the reviewer's *"a non-empty first draw ⇒ `allocated == true` ⇒ `!allocated` never fires"* holds **only for a model's first-ever grant**, and its residual — *"draws after the first (where `capN == 0` now genuinely defers, because `allocated` is already true — the intended behavior)"* — **conflates a later draw *within* a call with a later *outer iteration*.** `allocated` is per-call progress, not a persistent flag: a model already holding replicas whose entitlement floors to 0 for every role in some later iteration makes no progress, so `!allocated` fires at `:301` and `w.remaining = -1` drops it from `active` **permanently**. That is the same eviction, and it is unprotected. **Why this matters for the number rather than only for the mechanism:** the coder's collapse deltas were `3→1`, `2+→1` and `bv` **6→2** — models that *did* receive replicas and then stopped, i.e. precisely the population `firstDraw` does **not** cover. So the mitigation is real but is concentrated on the case least represented in the measured failures. **Consequence for my own B10 reversal: both of its legs are weakened by this one fact** — the measurement is stale (reviewer), and my pool-vs-entitlement asymmetry argument is mitigated exactly where it applies least (mine). I am therefore **suspending the `ceil` recommendation rather than flipping back to `floor`**: a third position on the same question, argued rather than measured, is worth less than waiting for a number, and the reviewer explicitly signs off no count. **Falsifiable prediction, offered so the refresh can check me:** `floor` + C6e should still show collapses among models already holding replicas, but **fewer than 9**. **The refresh is already in flight** — the coder's worktree has an **uncommitted** `math.Ceil → math.Floor` at `:837` (committed HEAD still ships `ceil`; `git status` shows `M greedy_score_optimizer.go`), which is exactly the experiment the reviewer described as cheap. I read only the committed object once I saw the tree was live-edited, ran nothing, and touched nothing. **A third option neither fork side captured, worth putting in front of Dean:** `floor` + the shipped guard is a *hybrid* — `ceil`-like on a model's first grant, `floor`-like thereafter — which would bound the Σ-entitlement overshoot (**B8**) to one replica per model per water-fill instead of one per draw. If the refreshed count is small, that is a strictly better answer than either fork side, and it is the one the branch already almost implements | reviewer's stale-input handoff §1; four links, the commit attribution, the `spentGPUs`/`targets` derivation and the uncommitted state all verified by me at `a9afb740` |
| A53 | §4a reword ledger — **supersedes A46's numerator: the window is 22 of 25, not 21** | **The coder corrected its own two figures and conceded one message too many; the concession does not survive its own commit's stated rule.** Both corrections in its handoff I had already reached independently an hour earlier (A46): the denominator is **25**, and `a9afb740` is **not** exempt — its body carries the token list, mention-not-use, but §4a has no quoting exemption. Agreed on both, and the reword there is **lossless**, as the coder says: the message already writes *"plan-item labels"* and *"per-commit labels"* one clause earlier, so the two parenthesised enumerations simply drop. The `T1-ols` paragraph and `throughput/constants.go:129` **stay** — verified: that line is `itlReasonT1OLS = "T1-ols"`, consumed at `analyzer.go:598` and asserted at `analyzer_test.go:455`, i.e. a shipped identifier (A44's carve-out, confirmed from the tree). **Where I diverge, and it is the whole content of this row:** the coder reports the numerator as *"21 under the reviewer's rule and 22 under mine"* and then **concedes to 21**, excluding `b106b929` on the grounds that its only hit — *"PR-1's Test 9"* — is *"a PR reference [which] resolves from `main` — the same reasoning that kept `#1513`."* **That reasoning does not transfer, and `a9afb740`'s own message is what shows it:** C9e states the criterion as *"'#1513' is a **GitHub pull request**, which is exactly the kind of…"* — a resolvable number. `PR-1` is not a GitHub number; it is the plans-branch **mission ordinal** for what became #1516. Verified three ways: `PR-1` appears **nowhere** in `main`'s Go source; `Test 9` appears **nowhere** in `internal/` (so it is a plan-doc test ordinal, not a test name a reader can find); and a per-commit inventory over `rev-list 075a208e..HEAD` counting `PR-N` ordinals returns **22**, with `b106b929` the **only** commit whose sole hit is one. So the entire dispute reduces to one message under one criterion, and the criterion is supplied by the very commit that conceded it. **Fix in `b106b929` is as cheap as C9e's:** *"Updates the two-analyzer combine characterization test (PR-1's Test 9)"* → name the PR (**#1516**) or drop to prose (*"the two-analyzer combine characterization test added by the anchor refactor"*). **Ledger for Dean: 22 of 25** — the cost of waiting is now **22 now vs 26+ later**, still a narrower gap than the 9-vs-16 originally queued, so this does not strengthen the case for rewording now; it only makes the figure right if he takes it. Note the shape once more: this is the **fourth** consecutive round in which a §4a instrument certified as exhaustive proved under-inclusive at its own boundary — `4fb49ac6` matched paths, the reviewer's filter matched comment syntax, my pattern matched four doc families, and the coder's rule matched "PR reference" without testing whether the reference resolves. Same defect, four instruments, which is B12's argument in its strongest form | coder's correction handoff §1–§2; the `#1513`-vs-`PR-1` criterion, both greps and the 22-count all verified by me at `a9afb740` |
| A52 | §C6c checklist — **two of five inventoried entries do not meet the criterion** | **Correct the inventory rather than the code.** `cost_aware_optimizer.go:160/163/172` and `rescale.go:380` are about the **scale-down score-weighted PRC tie-break**, which is an *ordering* key, not the fair-share metric — invariant 11 permits a ranking weight there, so they are **not false after C6c** and must not be "fixed." They were caught because they now *read* as contradicting C6c's new *"score takes no part in fair share"* prose. The coder's disposition is the proportionate one: one clarifying clause at the canonical definition site (`sortVariantsForScaleDown`) and at the dev-guide's copy, `rescale.go:380` left alone rather than duplicating the explanation at every call site. **Record both the removal from the checklist and the reason**, so a later pass does not re-add them — a checklist entry that was examined and rejected is worth more than one silently dropped | coder's close-out § also-worth-recording |
| A57 | §2f + §12 `(D-a)` ledger row — **`AD3`'s scoping and `(D-a)`'s deferral are coupled safety properties of `AD5`, and this row is the one that cannot wait on the scope call** | **The consequential finding of the round, and it exists only because Dean asked why I cared about prefill PRC — my first answer ("I shouldn't") was wrong.** Addendum 1's `AD3` scopes the from-zero PRC work to `decode`/`both` on the argument that all four pricing mechanisms are *"exactly equally inert on a prefill variant"*, prefill's blocker being a zero numerator. **The scoping is right; the rationale is false, and falsely reassuring.** Pricing a prefill variant is not inert — it removes a guard. Verified at `a9afb740`: `scaleDownVariantSet` (`cost_aware_optimizer.go:124`) skips a variant on `if vc.PerReplicaCapacity <= 0 { continue }`. **⚠ CORRECTED this round — the internal reviewer caught it and I confirmed it on the decisive link: that skip declines the reclaim only for a *zero-replica* prefill variant, never a live one.** `analyzer.go:364` gates *only* the decode ITL/OL accumulation on `state.role != RolePrefill`; `PerReplicaCapacity: perReplicaSupply` at `:398-408` is **not** role-gated, so **TA already prices every live prefill variant** and the skip is already not firing anywhere on the `AD5` path. The `:292-302` "binder omits this variant" comment describes a variant the binder *cannot* price — i.e. one at zero replicas — which is the case I over-generalised. **Nothing protects a running prefill role** (see A63 for what actually happens to it). The coupling recorded below is therefore real for the **from-zero corner only**, and is **not** load-bearing for `AD5` as scoped Give a prefill variant `PerReplicaCapacity = 1` — exactly what the deferred `(D-a)` `Reason`-tagged `FZ-admission` sentinel does — and the demand stays 0 (vote set still empty, `combineVotes` still `(0, -1)`), while the skip stops firing: `removable = current - minReplicas`, and the role sheds to the cheapest-at-1 positional protection or to zero. **So for a variant at zero replicas the two independent effects of the same `PerReplicaCapacity = 0` cancel** — demand stays 0 *and* the skip fires — and `(D-a)`'s deferral plus `AD3`'s scoping hold that cancellation in place. **What the correction removes is the scope of the claim, not the claim:** this is a from-zero property, and it does *not* describe the `AD5` fixture (prefill at ≥ 2 replicas), where the skip never fires. Two consequences I still own, both narrowed to the from-zero case: (1) **A35's five-part §4b classification for `(D-a)` needs a sixth element** — for a zero-replica prefill variant the deferral is *protective*, not merely "no consumer yet", so a future session reading it as unblocked scope-creep would un-defer it without knowing that much is load-bearing; (2) **if `(D-a)` is ever un-deferred, a from-zero prefill variant becomes reclaim-eligible the moment it is priced** — an ordering recorded nowhere today, and the one place `AD5`'s predicate and `(D-a)` genuinely interact. **This row stays unconditional** — it is worth recording whichever way Dean rules B15 — but it is **no longer the condition that makes deferring B15 safe**; A63 is The `AD3` rationale itself is inside the designer's addendum — amending it is theirs, sent as `designer__ad3-rationale-false-…` §1; the coupling record in the Type 3 is mine | mine, from Dean's "why do you care about prefill PRC for TA?"; both skips and the cancellation traced by me at `a9afb740` |
| A56 | §2f `AD5` transcription — **the cited mechanism is base-`075a208e` code; the operative one is the anchor's prefill PRC acting twice** | **The conclusion holds, both proposed mechanisms do not — including my own first correction.** `AD5` cites `roleDemandGPUs` reading `demand = rc.TotalDemand` off `anchor.RoleCapacities[prefill]` at `rescale.go:554-570`. Exactly right at base `075a208e`; **false at HEAD** — bug #3 (`07b8fdb7` + `3c9d45bb`) added the ballot as a parameter, so at `rescale.go:579` demand is `combineVotes(votesFromTotalDemand(s, role, bestVariant), true)`, per voting entry, with `s := votingResults(req.AnalyzerResults)` at `:360` confirming the pruned set reaches the role math. Coordinates drifted too (`:554-570` → `:579`). **My own abstain-on-empty-vote-set correction is also not operative:** `roleDemandGPUs` skips `vc.PerReplicaCapacity <= 0` when choosing its reference variant, so `bestVariant == ""` and it **returns 0 before the vote combine runs at all**. The anchor's prefill PRC is 0 because the binder omits those variants — stated in the merge's own comment at `analyzer_helpers.go:287`. **Placement consequence, matching the coder's: a hold predicate cannot go where `AD5` points**, because the anchor's `RoleCapacities` no longer sizes a role; it belongs where the ballot is built or where `binder < 0` is handled. **Two severity facts a reader will otherwise get wrong, both verified: the floor does not protect the role** (`roleFloorGPUs:638` is `minReplicas × GPUsPerReplica`, hence 0 whenever `minReplicas` is unset — the common case — so `rt < rc` fires and `reclaimRole` is called for the role's *entire* allocation), **and the scale-down gate does not either — though ⚠ my first statement of *why* was scoped to the wrong function and is corrected here.** `scaleDownVariantSet` is a *helper parameterised by its caller's `maxRemovable` callback*, so whether a gate applies is the caller's property, not its own, and the two callers differ: `reclaimRole` (`rescale.go:389`) passes pure GPU arithmetic (`remaining / g`) and consults **neither** gate, while `scaleDownRoleIterated` (`cost_aware_optimizer.go:468-508`) gates the loop on `needsScaleDownForRole` **and** passes `safeRemovalReplicasForRole` as the callback. **Where the gates are consulted they pass rather than protect** — prefill `SpareCapacity` is the *full* prefill supply (its demand is structurally 0, see A63), so no spare veto fires, and `safeRemovalReplicasForRole` computes `floor(RoleSpare[prefill] / prc) ≈ the entire prefill fleet`. `AD5`'s path is `reclaimRole`, i.e. the ungated one. So §2f must transcribe the *effect* with HEAD's mechanism, not the addendum's citation | designer's `AD5` + coder's `plan__…ad5-cites-main-not-pr2…` §2; chain re-traced by me at `a9afb740` |
| A58 | §2b `N7` prose — **`N7` fails safe by abstaining, not by vetoing; same disposition, inverted mechanism** | **Third correction to Addendum 1 in one sitting, and the mechanism matters more than the verdict.** The addendum dispositions `N7` on the grounds that a role key missing from one live analyzer *"reads as zero spare, hits `needsScaleDownForRole`'s `<= 0` branch, and vetoes."* At HEAD (`analyzer_helpers.go:891`) that function reads `if _, ok := e.RoleSpare[role]; !ok { continue }` — commented *"this analyzer doesn't decompose this role; abstain, not veto"* — and returns `liveCount > 0`. That is the `N2`/`N7` abstain-vs-veto resolution **PR-2 already shipped**. **The disposition survives unchanged** (sat pruned + TA abstaining ⇒ `liveCount == 0` ⇒ `false` ⇒ a role never scales down rather than spuriously), but it holds because the live count is zero, not because a missing key vetoes. **Worth correcting because the veto framing makes A57's teardown chain look impossible:** a reader who believes a missing key vetoes concludes the prefill reclaim is blocked by the scale-down gate. It is not — and per A56 that gate is not even on the reclaim path. §2b's prose must state the abstain mechanism, since it is the one PR-2 implements and documents | designer's candidate-follow-up `N7`; HEAD mechanism verified by me at `a9afb740` |
| A60 | plan design-authority line — **name Addendum 1 alongside the frozen parent, with its precedence rule** | **The designer's one procedural ask, and the cheapest row in the batch: today the addendum is reachable only by filename.** `combined-analyzer-optimizer-design-addendum-1.md` (Type-1 Addendum 1, `AD1`–`AD7`, 11 sections, TOC-indexed) is not linked from the frozen parent, because Dean's *"no editing"* (2026-08-08) leaves `combined-analyzer-optimizer-design.md` @ `8c2a9b04` reading as if its § open queue were the complete picture. The designer cannot fix this — the two documents that could carry the pointer are the Type-3 plan (mine) and CURRENT.md (sync's). **Fix: the plan's design-authority line names both files and carries the precedence rule — where the two overlap the addendum is later and governs** — plus a `sync__` line so CURRENT.md's design-authority reference covers both. **This is the one row I would take ahead of the batch:** it is a pure pointer addition with no behavioral content, and every round spent re-deriving which document governs is a cost the pointer removes | designer's discoverability ask; accepted and confirmed back |
| A61 | C9 dev-guide pass — **`AD2`'s doc line must record *why* the `[TA]`-only-on-P/D constraint is unenforceable, not just the constraint** | Dean's ruling (*"you should not use TA only on P/D. The default should be to use both. This can be documented -- as long as we only have TAdec we cannot use it as the ONLY analyzer"*) is **documentation, not enforcement**, and the reason is structural rather than expedient: analyzer enablement is fixed from controller config while roles arrive at runtime from VA specs (`VariantReplicaState.Role`), so **at the moment the analyzer set is chosen, no role is known**. A real check would be a per-model runtime warning on first observing a prefill-roled variant under a TA-only config — separate work, not a config-schema addition. **So the constraint ships unenforced by design, and the dev-guide line must say so**, otherwise the next reader files the missing validation as an oversight and the round is spent re-deriving `AD2`. Placement in C9's dev-guide pass is mine (the designer explicitly left it to me); the ruling is Dean's | designer's `AD2` (Dean's ruling); placement + rationale-must-ship mine |
| A62 | §2f — **`AD3`'s domain statement lands with the substitute rationale, not the inertness one** | `AD3` is a domain statement for the spec, not a guard anyone writes: the from-zero PRC work is scoped to `decode` and `both`, and it narrows what `FZ-admission` is claimed to cover without reopening its mechanism. **Transcribe the scoping — but per A57 the stated reason cannot be transcribed with it.** Substitute, no new mechanism: prefill demand is zero for reasons upstream of pricing, **and** pricing a prefill variant is *not* inert, because it removes the unpriced-skip that currently declines the reclaim. Writing the inertness version into my Type 3 would propagate the one false premise that licenses relaxing the scoping as harmless — the §2f transcription is exactly where a future reader would meet it | designer's `AD3`; substitute rationale from A57 |
| A59 | §2f + residual table — **the designer's self-flagged unverified row is closed; the residual does not widen** | The designer declined to assert saturation's tier-2 (own-`CapacityRecord`) from-zero pricing rung on the reviewer's authority alone, flagging two preconditions as unverified and stating that if either blocks a zero-replica variant *"that row weakens and the residual widens to include `[sat]`-only."* **The coder checked both and neither blocks.** Five specific checks, all read-only: `engine_v2.go:38-53` iterates `variantAutoscalings`, **not** `replicaMetrics`, so a zero-replica variant is still visited; the `scaleTarget == nil` skip at `:42` keys on the accessor, which exists at `replicas: 0`; the params are spec-derived via `ParseEngineArgs` → `ParseVLLMArgs` reading a **pod template**, not a live pod; `capacity_store.go:126-128` exists for exactly this case; and `EffectiveMaxBatchedTokens > 0` is guaranteed by an 8192 fallback. Plus `LoadFromScaleTarget:98-101` preserves a `learnedFromLive` record until `EvictStale` at 24h. **So the residual stays as the addendum has it, and `[sat]`-only is *not* added.** Record it on the coder's evidence and credited to it — the designer's instinct not to assert it unverified was right, and the closure is worth more than the assertion would have been | coder's `plan__…ad5-cites-main-not-pr2…` §3; designer's flagged row |
| A63 | §2f `AD5` transcription + the basis of B15 — **`AD5` is an active prefill drain, and it reproduces at base `075a208e` unchanged: PR-2 neither introduces it nor widens it** | **The row that decides B15, and it arrives by overturning both the severity and the attribution the round had settled on — mine downward, the reviewer's sideways.** Mechanism traced end-to-end at `a9afb740`, every link read-only: `distributeDemandByRole` (`analyzer.go:889-911`) never adds prefill to its `roles` set (`if role != domain.RolePrefill`) and divides demand only among the roles it did add — its own comment saying why (*"both are decode-rate-denominated and role-agnostic at the point they are computed, so prefill roles are excluded"*) — so `arrivalDemandByRole[prefill]` is **absent, and absent by construction rather than by accident**; `aggregateRoleCapacities` treats the nil lookup as zero; the engine's universal threshold post-step therefore writes prefill `SpareCapacity` = its **entire** supply; `rescaleModelDecisions:369` reads `demByRole[prefill] = roleDemandGPUs(...) = 0`; `distributeGPUsByWeight` gives prefill **weight 0**, collapsing `tgtByRole[prefill]` to `floorByRole[prefill]` = `minReplicas × GPUsPerReplica` = **0 with `MinReplicas` unset**; `rt < rc` fires and `reclaimRole` is called for the role's *entire* allocation. **Symptom, and the reviewer's characterisation is right where mine was wrong: prefill is actively shed to its floor while decode scales normally, with the prefill gauge reading 0.** Not a freeze, not a missed scale-up — a drain, in steady state, needing no contention. **Attribution is the load-bearing half, and it is what makes the defer correct on principle rather than on convenience.** Three base facts, each verified at `075a208e`: (1) `distributeDemandByRole`'s prefill exclusion is **byte-identical** to HEAD's; (2) base `roleDemandGPUs(anchor, stateMap, accType, role)` (`rescale.go:545`) read prefill demand off the **anchor** — the ballot parameter is PR-2's bug #3 addition; (3) base `bindingAnchor`'s binder gate is **already** `Enabled && Live && ResultIsInformative`. Compose them against the reviewer's own fixture (sat `Enabled: true, Live: false`, TA binding, prefill ≥ 2 replicas, no `MinReplicas`): base already fell through to the non-saturation branch and **already bound TA**, already read prefill demand **0** off that anchor, already gave prefill weight 0, and already called `reclaimRole`. **The drain is inherited in full.** ⚠ **This corrects the reviewer's one PR-2-attributable claim:** `reclaimRole` is *not* "newly unmasked by `VG-up`" in this fixture. `VG-up` prunes `votingResults`, which reaches `reclaimRole` only as (a) `sortVariantsForScaleDown` **ordering** and (b) bug #3's new `s` argument to `roleDemandGPUs` — and neither can push prefill's demand below the 0 it already held at base. If anything PR-2 helps: the `s` argument gives prefill demand a *second* chance to be non-zero, since a live non-saturation analyzer that does price prefill now contributes where base's anchor-only read ignored it. **Consequence: `AD5` is a pre-existing defect of the opt-in TA path on P/D models under saturation outage, not a PR-2 regression** — so it belongs in a follow-up issue beside `AD7`/`N5` and the QM contract item, and B15's defer stops resting on "the window is narrow" and starts resting on "this is not PR-2's bug." ⚠ **EXTENDED after the coder's execution run — `AD5` has *two* paths, and both are inherited, by different chains. This is the form to transcribe.** **Path A — steady-state, now CONFIRMED by execution:** `scaleDownRoleIterated`'s `needsScaleDownForRole` + `safeRemovalReplicasForRole`. Attribution verified at base by me: base `needsScaleDownForRole` returns **true** in this fixture exactly as HEAD does — TA is live and `RoleSpare[prefill]` is the *full* prefill supply, so base's inline `RoleSpare[role] <= 0` veto never fires and `liveCount == 1`; PR-2's changes to that function (hoisting `roleSpareVetoed`, abstain-on-missing-key) **cannot bite here because the key is present and positive**. Base `safeRemovalReplicasForRole` is identical, and — the cleanest part — **both functions carry their own `!e.Live` skip, so `VG-up`'s ballot pruning is irrelevant to this path entirely.** **Path B — rescale, still PLAUSIBLE only:** `roleDemandGPUs` → weight 0 → target collapses to floor → `reclaimRole`, inherited by the three base facts above. **So PR-2 authored neither path.** ⚠ **And the confirmed run took Path A, not B — the coder's unexplained measurement is the evidence.** It measured prefill → **1, never 0, at every starting height**; Path B predicts **0** when `MinReplicas` is unset, so a floor of 1 is inconsistent with B and consistent with A plus a last-replica protection. That resolves the coder's own open item at zero cost, and it means the **worse** of the two paths (B, which goes to 0) is the one still unconfirmed. The exact clamp producing 1 stays unlocated — the coder stopped rather than widen unasked, correctly. **Evidence status, stated so it is not overclaimed:** HEAD behaviour is now **CONFIRMED by execution** (coder's scratch diagnostic, since deleted; tip unchanged, 386 specs green). **Base is still a reading claim** — five base functions (`distributeDemandByRole`, `roleDemandGPUs`'s signature, `bindingAnchor`'s binder gate, `needsScaleDownForRole`, `safeRemovalReplicasForRole`) all composing to the same drain, but nobody has *run* the fixture at `075a208e`. **That is the one cheap experiment that would settle authorship outright**, and it is a coder action in the coder's tree — worth offering to Dean, not worth directing. ⚠⚠ **CORRECTED AGAIN 2026-08-08 — Path A's inheritance is CONDITIONAL, not flat, and my "the drain is inherited in full" was an overclaim. This is the form to transcribe; the paragraph above stands only for Path B.** The reviewer found the link neither of us checked — **`scaleDownRoleIterated`'s *reachability* is governed by a dispatch that reads the ballot with no liveness filter** — and I verified all four of its links myself rather than accepting them. (1) The dispatch is **shape-identical** at both revisions: `s := votingResults(...)` → `initRoleState(s)` → `if anyRoleNeedsScaleUp(ps, roles) { allocateForModelPaired } else { scaleDownRoleIterated }` (base `cost_aware_optimizer.go:60-67`, HEAD `:59-66`), so the **only** thing that differs there is what `votingResults` returns — which is exactly `VG-up`. (2) Base `initRoleState:271-297` applies **no liveness filter**: it skips only `e.Result == nil`, then writes `pickerState[i][role] = rc.RequiredCapacity` for every remaining entry, **dead ones included**. (3) Base `anyRoleNeedsScaleUp:357-366` is a **global OR over every entry × every role** — `if m[role] > 0 { return true }`. **Compose: at base, a dead-but-`Enabled` saturation whose stale `RoleCapacities` holds *any* role with `RC > 0` sent the model down the *scale-up* branch, so `scaleDownRoleIterated` was never reached — no drain at base.** `VG-up` prunes that entry, TA is left alone, prefill's `RC` is structurally 0, and the dispatch falls through to scale-down. **So Path A is inherited only when the dead analyzer's final snapshot carries no positive `RC` on any role — and because the OR spans roles, a positive *decode* `RC` alone suffices to divert, which makes the diverting class far broader than "prefill needed scale-up."** For a saturation that dies holding any positive `RC`, **PR-2 converts base's *"scales up on stale data"* into *"sheds prefill to one replica"*: machinery inherited, reachability in that reconcile not.** ⚠ **This also refutes a specific thing I wrote:** my counterfactual dismissal — that the protection `VG-up` removes *"never existed in any shipped state"* — is correct for Path B's demand-weight route and **wrong for Path A**, where the masking **did** ship, via the dispatch. Base's behaviour there is its own bug (it scales up on stale data), so this is bug-masking-bug again — but unlike Path B's case, **this masking bug is in the shipped base**, which is the whole difference. (4) **The floor-at-1 now has a named mechanism, and it confirms my Path-A-not-B inference:** `scaleDownVariantSet:157-161` is `#1237`'s cheapest-at-1 positional rule — last/cheapest variant, `current-n < 1`, no more-expensive variant still holding replicas ⇒ `n = current - 1` — so a single prefill variant floors at **exactly 1 from any height in one pass**, which is the measured table; `reclaimRole` has no such rule and would give **0**. **Measuring 1 rather than 0 *is* the path instrumentation**, closing the coder's open item. **The pairing to carry forward, because it is the uncomfortable shape of this whole finding: the path whose attribution is unconditionally "inherited" (B) is the one nobody has run, and the executed path (A) is the one whose attribution is conditional.** The reviewer's settling experiment is the right one and is cheaper than either of us re-reading: run the coder's fixture at **base** twice — stale sat `RoleCapacities` all-zero, then with any one role positive; prediction is base drains in the first and **not** in the second, HEAD drains in both. ⚠⚠⚠ **THIRD PASS 2026-08-08 — the base run happened, and the condition above is now traced to its root. Net: the conditional framing is structurally right but the condition is *unreachable with the shipped analyzer set*, so "inherited" recovers almost all the weight it lost. This is the form to transcribe.** **(a) What the coder's base run does and does not show.** It measured base = HEAD exactly — prefill **1** from 2, 4, and 8, both optimizers, decode holding as control — and reports base's pipeline suite at **308** specs vs HEAD's 386, run in a throwaway detached worktree (`git worktree add --detach /tmp/ad5-base 075a208e`), diagnostic deleted, no branch moved. **But its handoff title (*"attribution settled"*) overstates its own result, and the gap is a field name:** its §2 refinement gave the dead saturation entry a non-zero stale **`TotalDemand`**, which is the field the *demand* path reads (`votesFromTotalDemand` → Path B). The reviewer's diverting condition keys on stale **`RoleCapacities[role].RequiredCapacity`**, which is what `initRoleState` copies into `pickerState` and `anyRoleNeedsScaleUp` ORs over — a **different field on a different path**. So the run instantiates the reviewer's **variant 1** (dead sat contributing no positive `RC`), where base was *predicted* to drain — and it drained. **It confirms the tie for this fixture and confirms Path B's counterfactual empirically; it does not refute the dispatch condition, because the fixture sits on that condition's true branch.** Variant 2 remains unrun. **(b) The reachability of the condition, which neither the reviewer nor the coder traced — verified by me at base, read-only, and it is the part that actually settles this.** `updateLivenessAndSetLive` (`saturation/engine_v2.go:206-247`) sets `nr.Live` **in place on the current cycle's `namedResults`**; no prior cycle's `Result` is carried forward, so a `Live=false` entry's `Result` is whatever *this* cycle produced. The latch is `nr.Live = ok && now.Sub(lastGood) <= threshold`, with `lastGood` refreshed to `nr.Result.AnalyzedAt` (or `now`, via the explicit fail-safe for a zero timestamp) **whenever `ResultIsInformative(*nr)`**. And **every shipped analyzer stamps `time.Now()`** — queueingmodel `:142`, sat_v2 `:130`, TA `:475` (`now`), saturationv1 `:44`/`:56`; the *only* non-`now` assignment in the tree is `analyzer_helpers.go:176`, which is the **anchor** construction copying its carrier's timestamp and never becomes a ballot entry feeding the latch. **Therefore an informative result always refreshes the latch to ≈`now`, so `Live == false` implies this cycle's result was non-informative.** `ResultIsInformative` (`analyzer_helpers.go:53-63`) is true iff *some* `VariantCapacities` entry has `Reason` neither `ReasonNoData` nor `ReasonError` — so non-informative means **every** variant is NoData/Error. And `RoleCapacities` is an **aggregation over `VariantCapacities`** in both producers (sat_v2 `:136` from `roleCapacities`, TA `:481` from `aggregateRoleCapacities(variantCapacities, …)`, with `aggregation.go:20-26` stating the contract that the role sums equal the per-variant sums filtered by `vc.Role`). **So the reviewer's diverting class requires an analyzer to report "no data" on every variant while simultaneously aggregating a positive role `RequiredCapacity` — i.e. exactly a break of the "dead ⇒ `RC = 0`" invariant.** **(c) That invariant is already tracked, by name, as this branch's own hardening trigger.** It is the documented basis of Dean's PR-1 `D1`/`D2` deferral: scale-up safety is *emergent* (rests on dead ⇒ `RC = 0`), not *enforced*, with the named trigger being *"a future analyzer that carries forward stale-but-informative `RC > 0` with an aged `AnalyzedAt`."* Two things follow. **The aged-`AnalyzedAt` half of that trigger is not reachable today** — I raised it as a worse branch than the reviewer's and it is closed: no shipped analyzer emits an aged timestamp, so the "informative result with `Live=false` in the same cycle" shape cannot occur. **And the reviewer's dispatch class is the *other* half of the same already-recorded trigger**, not a new hazard: it is unreachable for the current analyzer set and becomes reachable exactly when `D1`/`D2`'s enforcement gap is exercised. **(d) Net attribution, and the one link I have *not* verified.** Path A is inherited for every dead-analyzer shape the shipped analyzer set can produce; the diverting class needs an invariant break already on this branch's hardening list. **The residual: I verified the aggregation *contract* and both producers' construction sites, not each producer's per-variant fill — so "a NoData/Error variant cannot carry positive `RequiredCapacity`" is emergent-and-unverified rather than proven.** Stated as a caveat, not smuggled in as a fact; it is a two-function read if anyone wants it closed. **(e) Stale item to ignore:** the coder's §3 still lists the floor at 1 as unexplained — the reviewer's §3 explains it (cheapest-at-1, `scaleDownVariantSet:157-161`) and cc'd the coder, so that item is already answered and needs nothing from me. ⚠⚠⚠⚠ **FOURTH PASS, same day — the reviewer retracted its path-instrumentation inference and the retraction takes one of my claims with it. Verified at base before accepting.** **(f) What is falsified.** The reviewer's §3 (which I adopted as pass-two item (4)) held that `reclaimRole` *"has no such rule — pure GPU delta, would give 0",* so measuring 1 identified the path. **False, and I confirmed it at base:** `reclaimRole` (`075a208e:rescale.go:389-411`) sheds nothing itself — it calls `scaleDownVariantSet` with a `remaining / g` `maxRemovable` callback, and the cheapest-at-1 rule lives **inside that helper** (base `cost_aware_optimizer.go:142`, byte-identical to HEAD `:157-161`), so it fires for **both** callers. **Both paths floor at exactly 1, therefore the outcome value cannot distinguish them and which path either run took is UNKNOWN — on both sides.** My Path-A-not-B inference is withdrawn in full; the coder's *"I did not instrument which gate either run took"* was the correct posture and both the reviewer and I overreached past it. **Worse, and worth recording rather than smoothing: `reclaimRole`'s own doc comment says *"respecting minReplicas and the cheapest-at-1 protection, via scaleDownVariantSet"* — base `rescale.go:387-388`, byte-identical — so we each asserted the negation of a sentence written in the function we were reasoning about.** **(g) The same omission corrects a claim of mine that predates the reviewer's.** My §2 / this row's earlier passes predict `reclaimRole` reclaims to **0** when `minReplicas` is unset. It does not — the helper caps it at 1. So every *"sheds to its floor = 0"* / *"the role's entire allocation"* formulation in A56 and in this row's earlier passes must read **"calls for the entire allocation, and the helper floors the outcome at 1."** The call breadth was right; the outcome was not. Severity is materially unchanged (an 8-replica tier still sheds 7) and A65's measured *"lands at 1"* was right all along — but the **cheapest-at-1 positional rule is the thing standing between `AD5` and an actual scale-to-zero of a serving role**, which is worth naming in the follow-up text as an accidental protection nobody designed for this purpose. **(h) What is untouched, and is now the whole basis.** Pass three's **(b)/(c) reachability trace stands entirely** — it concerns whether a dead entry can carry positive role `RC` *in production*, which is independent of which drain path executes once scale-down is reached. Attribution therefore rests on (b)/(c) alone now, not on path identity. **(i) A distinction the reviewer's proposed refutation makes necessary, stated as a falsifiable prediction before anyone runs it.** Its §2 supplies the link I had not made: a hand-built `AnalyzerResult` handed straight to an optimizer **bypasses `applyUniversalThreshold`** (`saturation/engine_v2.go:496-512`), the only code deriving per-role `RC` from per-role `TotalDemand`. So a *fixture* can hold combinations no *analyzer* can emit — NoData/Error on every variant **and** a positive role `RequiredCapacity` — because production `RC` is aggregated from variant capacities inside the analyzer. **Therefore its experiment tests the dispatch as a code property, while (c) tests production reachability, and both answers can be "yes" without contradiction. My prediction, recorded now so it is falsifiable: base *stops* draining in that run — the dispatch reading is correct and the reviewer should not have to retract it — *and* production reachability remains nil, gated behind the already-tracked `D1`/`D2` invariant.** If base still drains, the dispatch reading dies and `AD5` is unconditionally inherited, which strengthens B15 rather than weakening it; either outcome leaves B15's recommendation where it is. **Also confirmed by that read: the cheapest-at-1 rule is present at base, which is why both sides measure 1 at every height whichever path ran**. ⚠⚠⚠⚠⚠ **PASS FIVE, same day — the contended path is measured too, and `AD5` is inherited in full.** **(j)** The coder swept the *contended* path (`Optimize` with rescale enabled, a hog and a contended pool, contention held identical between the two columns) across twelve configurations, and base `075a208e` matched HEAD `a9afb740` **byte-identically on every row**. With (a)'s steady-state tie this closes the coder's own §3 caveat and confirms the claim I made ahead of the evidence — `AD5` is inherited in **both** of its paths — so **no part of `AD5` is a PR-2 regression, and `952d2fff` authored none of it**. **(k)** The run's title again reaches past the run, and the coder says so itself: it did **not** instrument that `reclaimRole` produced the numbers (*"identical-at-base is identical whichever function did it"*), which is consistent with pass four — path identity remains unknown on both sides. And the dead entry again carried a stale **`TotalDemand`**, not `RequiredCapacity`, so the reviewer's §3 one-line refutation is **still untested**, and my prediction in (i) stays open rather than resolved. **(l)** The refutation is one round stale, and that is a convergence rather than a dispute: both claims in the `designer__ad3-…-not-a-freeze` §2 second bullet — *"newly unmasked by `VG-up`"* and *"the whole allocation is reclaimed"* (floor 0) — had **already been withdrawn by the reviewer itself**, the first in `…inherited-carries-a-condition-the-dispatch`, the second in `…correction-floor-at-1…`. Stated plainly for the record: **the reviewer's *"whole allocation is reclaimed"* and my *"reclaims to 0"* are the identical omission in the identical function**, so pass four (g)'s correction covers both — three roles made the same two mistakes, and a measurement plus a re-read closed them. **(m)** The coder still lists *"why the floor is 1 and not 0"* as unexplained; it is **answered** (cheapest-at-1 inside `scaleDownVariantSet`, present at base `cost_aware_optimizer.go:142`) by a finding that post-dates its run — do not carry that residual forward. ⚠⚠⚠⚠⚠⚠ **PASS SIX, same day — the refutation experiment ran, my prediction was half right, and the wrong half is the one that carried the attribution. `AD5`'s attribution is now REGIME-SCOPED, and pass five (j) is withdrawn. This is the form to transcribe; every earlier "inherited in full" in this row is superseded.** **(n) The result.** The coder ran the reviewer's §3 experiment with both attached conditions honored — dead entry's per-role `RequiredCapacity` positive (decode alone), `TotalDemand` untouched, path fixed by construction rather than by counter (rescale off ⇒ `applyRescale` unreachable). Base `075a208e` vs HEAD `a9afb740`, n ∈ {2,4,8}, both optimizers, steady and contended: with `deadRC = 0` base ≡ HEAD (prefill 1); with `deadRC > 0` **base preserves prefill at n and scales the model to 2n, HEAD drains prefill to 1**. **So the split is by regime, not by path** — retire the path-based framing on all three sides, mine included. **`deadRC = 0`: inherited. `deadRC > 0`: HEAD-only — `VG-up` (`952d2fff`) opens the reachability.** Pass five (j)'s *"no part of `AD5` is a PR-2 regression, and `952d2fff` authored none of it"* is **withdrawn**; it was true of the regime the fixtures had been supplying and false of the one they had not. The coder retracted the same sentence from its own handoff, unprompted and against its own commit — worth recording as the reason this round converged at all. **(o) My prediction in (i), scored.** First half **CONFIRMED**: base stops draining, the dispatch reading is correct, and the reviewer does not have to retract it. Second half — *"production reachability remains nil, gated behind the already-tracked `D1`/`D2` invariant"* — **FALSIFIED**, and falsified at precisely the residual I had flagged as emergent-and-unverified in pass three (d). The caveat was the right instinct and the answer went against me; the honest reading is that (d) was load-bearing, not decorative. **(p) The falsification, traced by me at base `075a208e`, read-only, and it starts by correcting a claim of mine.** Pass three (b) asserted *"`RoleCapacities` is an **aggregation over `VariantCapacities`** in both producers."* **That is false for the demand terms, which is the whole hinge.** TA's `aggregateRoleCapacities` (`internal/engines/analyzers/throughput/analyzer.go:924`) takes **model-level** demand maps as separate arguments and its own comment is decisive — *"it no longer sums each role's per-variant computeDemand results … **RequiredCapacity and SpareCapacity are left zero — the engine's universal threshold post-step writes them**"*; sat_v2's `aggregateByRole` (`:467-503`) sums per-variant `TotalDemand` and then **adds `queueDemandByRole`** on top. And `applyUniversalThreshold` runs on the **saturation base result** (`saturation/engine_v2.go:123`) as well as on every enabled non-saturation analyzer (`:179`), deriving `RequiredCapacity = TotalDemand/scaleUp − TotalAnticipatedSupply` per role (`:468-502`). **So per-role `RC` is not aggregated from variant capacities at all — it is written by the engine from per-role demand, while `ResultIsInformative` (`analyzer_helpers.go:53-63`) keys *only* on `VariantCapacities[].Reason`. The two quantities are computed from different inputs, so "every variant NoData" and "positive role `RC`" are not mutually exclusive — they are independent.** I had verified the aggregation *contract* and both construction sites, not the bodies; the bodies are where it breaks. **(q) The coder's premise is wrong in mechanism and right in conclusion — both halves matter.** Its argument for realism was *"an analyzer entry that has just gone stale carries its last computed values."* **No such carry-forward exists:** `runAnalyzersAndScore` rebuilds `namedResults` from scratch every cycle with `if result == nil { continue }`, so a failed analyzer is **absent from the ballot** rather than held over, and the only state that persists across cycles is `e.lastGoodAnalysis` — **timestamps, not results**. The reachable positive `RC` is therefore **not stale at all: it is derived this cycle from live scheduler-queue demand.** That inverts the coder's §3 wording too — base is not *"scaling up on the strength of a dead analyzer's stale figures"*, it is scaling up on **half-fresh** data (real demand, absent capacity), which is a materially more defensible thing for base to be doing and makes HEAD's drain in the same reconcile the harder of the two to justify. Do not transcribe "stale figures". **(r) The reachable state, stated as a conjunction, with two narrowings that are mine.** All four must hold: **(1)** the model is disaggregated — `aggregateByRole` returns **nil** unless some variant's role is neither `""` nor `RoleBoth` (`:472-481`), which is `AD5`'s domain anyway; **(2)** every variant is `NoData`/`Error` — sat_v2's third branch (`:421-431`) needs `len(replicas) == 0` (no live metrics instances) **and** no store record with `EffectiveCapacity > 0`; **(3)** positive scheduler-queue demand on some role — **and `queueDemandByRole` is the *only* surviving demand term**, because per-variant `totalDemand` is declared fresh per variant (`:371`) and accumulated **solely inside** the `len(replicas) > 0` branch (`:389-398`), so an all-NoData cycle contributes exactly 0 from variants; no queue signal ⇒ role demand 0 ⇒ back in the inherited regime; **(4)** a live prefill fleet to drain — preserved, because `readyCount = vs.CurrentReplicas − vs.PendingReplicas` comes from **scale-target state, not from metrics**, so a fleet whose metrics have gone blind is still fully counted and fully drainable. **The narrowing that matters most is the opposite of a narrowing: one event supplies (2) and non-`Live` simultaneously.** `e.lastGoodAnalysis` is an in-process map and `CapacityKnowledgeStore` is `map[string]*CapacityRecord` behind a mutex (`capacity_store.go:36-46`) — **neither persists**. A **controller restart during a metrics outage** empties both at once: the store is cold ⇒ every variant NoData, and `perAnalyzer` has no entry ⇒ `nr.Live = ok && …` is false with **`ok == false` on cycle one**. **So the reachable instance needs no staleness window at all** — which corrects Finding 70 §2's closing *"sustained past `analyzerLivenessStaleCycles × interval`"*: that is required for the steady-outage route and not for the restart route. Net: **`deadRC > 0` is production-reachable — restart-or-outage blind metrics, cold store, queued requests, P/D model — not a fixture artifact.** Frequency stays unquantified, and the reviewer is right that a hand-built fixture carries no information about it. **(s) The consequence for the `D1`/`D2` deferral rationale, which is the part with reach beyond `AD5`.** Dean's PR-1 deferral rested on scale-up safety being *emergent* — "dead ⇒ `RC = 0`" — with the named trigger *"a **future** analyzer that carries forward stale-but-informative `RC > 0` with an aged `AnalyzedAt`."* **The break is not future and does not involve an aged timestamp: it is the shipped saturation analyzer, via the engine's own post-step.** Pass three (c)'s conclusion — *"the diverting class is unreachable with the shipped analyzer set"* — is **withdrawn in full**. What survives from (c) is narrower and still true: `Live == false` **implies** this cycle's result was non-informative (every analyzer stamps `now`, and the latch refreshes before it is read), so there is no "informative result with `Live = false`" state — which is exactly why the reachable route runs through non-informative-with-positive-role-`RC` and nowhere else. **(t) Trap one, and it is the reason (s) is not merely bookkeeping:** a regression test that *ages a timestamp* lands in `deadRC = 0` — the inherited regime — and **passes on base and HEAD alike**. Anything recorded with a test must construct the non-informative-with-demand state instead. (Trap two — that the obvious floor-at-1 cleanup makes `AD5` strictly worse — is A67.) **(u) Severity, absent the accidental brake, is teardown to zero.** The coder mutation-disabled the cheapest-at-1 clause (applied and reverted, nothing committed) and prefill went to **0** on both paths at all six rows; the mutation also failed 4 further specs in 386, so the clause is load-bearing well beyond this fixture. **What this does *not* license, and all three of us say so independently: it is not an argument to revert `VG-up`.** Base's behavior in the same reconcile is its own defect, and the choice is between two wrong states — which is Dean's to make, not a technical tie to be broken here. ⚠⚠⚠⚠⚠⚠⚠ **PASS SEVEN 2026-08-08 — the chain is now confirmed by execution, one of my formulations is superseded by a stronger one, and the last item reframes what the `deadRC > 0` harm actually is.** **(v) Executed, with numbers.** The coder called `applyUniversalThreshold` on precisely the state I traced — every variant `ReasonNoData`, positive role demand, no anticipated supply, shipped watermarks 0.85/0.70 — and measured `ResultIsInformative` **false before and after**, model `RequiredCapacity` 941.176, role `prefill` **470.588**, role `decode` **470.588**. The reachable conjunction is no longer a reading; it runs. **(w) Adopt the coder's formulation over mine.** I wrote that informativeness and per-role `RC` are *"computed from independent inputs"*. The sharper source fact: `applyUniversalThreshold` (`saturation/engine_v2.go:476-513`) contains **no reference to `VariantCapacities` anywhere**, so it cannot see `Reason` even in principle. The orthogonality is **by construction, not by data** — *any* non-informative result with positive role demand carries positive per-role `RC`, always. Use that in the record; my phrasing understates it as a coincidence of inputs. **(x) The all-roles refinement — checked, not transcribed, because it does not follow from uniformity alone.** The coder's dispatch fixture seeded **decode only** and it self-corrects to *"in the real route every role's RC is positive."* Uniform post-step application only gives positive `RC` to roles with positive **demand**, and with every variant `NoData` the surviving demand term is `queueDemandByRole` alone — so the claim reduces entirely to how queue demand is attributed, which is not what uniformity says. Verified at source: `estimateSchedulerQueueDemand` sets `byRole[prefill] = inputTokens` and `byRole[decode] = inputTokens + outputTokens` for a P/D model. Both positive; the refinement is **right**, and a fixture or Type-1 description of this state should say **all roles**, not one. **(y) A tightening that is mine and matters more than the refinement.** With no live replica metrics `computeModelWorkloadAverages` returns zeros (`count == 0`), so `tokensFromCount` and `outputTokens` are both 0 and `inputTokens` collapses to **`QueueBytes / BytesPerToken`** — a scheduler-side byte count, structurally independent of the engine-pod metrics whose absence produced the `NoData`. Two consequences: the surviving demand is not merely *"queue demand"*, it is the one signal an engine-metrics outage **cannot** take away, which is why the conjunction holds instead of self-cancelling; and prefill and decode come out **equal** in that limit, which is exactly the 470.588/470.588 shape measured — so the fixture is the **production limit**, not an approximation of it. **(z) The reframe, and it is the sharpest thing in this pass.** Every account so far, mine included, has read the `deadRC > 0` harm through `AD5`'s structural zero — *"prefill drains because nobody priced its demand."* **In the reachable state that is false:** the queue path prices prefill, equal to decode. What `VG-up` discards there is an entry that **correctly carried a positive prefill requirement**, computed this cycle by shipped code. So the `deadRC > 0` harm is a **distinct mechanism** from `distributeDemandByRole`'s zero, not an instance of it — which is why basis (2)'s carve-out cannot be waved through as *"the same inherited bug from another angle"*, and why a fix aimed only at prefill demand denomination **would not close it** | the retraction is the reviewer's `designer__ad5-correction-floor-at-1-does-not-instrument-the-path` (Finding 68, `a798dc87`), read here as cc; `reclaimRole → scaleDownVariantSet`, its doc comment, and the base cheapest-at-1 rule all verified by me at `075a208e`; the `applyUniversalThreshold` bypass point is the reviewer's, and it independently converges on my own (a) field distinction; the base run is the coder's `plan__…ad5-attribution-settled-base-drains-identically` (§1 table, §4 hygiene); the `TotalDemand`-vs-`RoleCapacities` field distinction, the liveness-latch trace, the five `AnalyzedAt` sites, `ResultIsInformative`, and both `RoleCapacities` producers all verified by me at `075a208e`; the `D1`/`D2` emergent-vs-enforced framing is Dean's own PR-1 decision; earlier rounds: reviewer's `plan__…ad5-mechanism-and-severity-verified`; coder's `plan__…confirmed-by-execution…`; reviewer's dispatch finding (`designer__ad5-inherited-carries-a-condition-the-dispatch`, Finding 67 `883c72d3`); all five base functions, the five HEAD links, and all four dispatch links verified by me at `a9afb740` / `075a208e`; the Path-A-not-B inference mine, its mechanism the reviewer's | reviewer's `plan__…ad5-mechanism-and-severity-verified`; coder's `plan__…confirmed-by-execution…`; all five base functions and the five HEAD links verified by me at `a9afb740` / `075a208e`; the Path-A-not-B inference mine; the contended 12-row A/B is the coder's `plan__…contended-path-also-inherited-review-2b-refuted` (whose own §3 states the no-instrumentation caveat); the `designer__ad3-…-not-a-freeze` §2 bullet read by me read-only, its two withdrawals traced by me across the reviewer's own later handoffs; pass six: the experiment and the mutation are the coder's `plan__ta-anchor-ad5-finding68-experiment-run-vg-up-does-open-reachability` (which retracts its own prior conclusion), the regime-split scoring and the two traps are the reviewer's Finding 70 (`e38e7d22`, `plan__ad5-regime-split-confirmed-plus-two-fix-scoping-traps`), and the reachability chain is **mine, verified read-only at `075a208e`** — `aggregateRoleCapacities`'s own comment (correcting my pass-three (b)), `applyUniversalThreshold` at both call sites, sat_v2's `aggregateByRole` + the `queueDemandByRole`-only demand term + the `var totalDemand` scope, `ResultIsInformative`, the no-carry-forward rebuild, `readyCount` from scale-target state, and the in-process `CapacityKnowledgeStore`/`lastGoodAnalysis` pair that yields the cycle-one restart route (correcting Finding 70 §2's staleness-window sentence) |
| A64 | §2f + C9 dev-guide — **bug #3 moved sizing onto the ballot and left observability on the anchor, so an `AD5` fix has two sites and only one is on the sizing path** | **Found by the reviewer, confirmed by me, and it is the row that stops the next scoping from fixing half of this.** `cost_aware_optimizer.go:350-367` still reads `anchor.RoleCapacities[role]` wholesale for `decision.RequiredCapacity` and `decision.SpareCapacity` (defaulting `role = domain.RoleBoth` when `state.Role == ""`), while sizing has moved to the per-voting-entry combine. So under `AD5` the published prefill `RequiredCapacity`/`SpareCapacity` are **0 regardless of what the sizing path is changed to** — a sizing-only fix leaves the CRD status still asserting that prefill needs nothing, on a role being drained. **Same failure character as B14's M1/M2 and A38's halved-decode telemetry: the system acts and the published state misdescribes why**, which is the class of defect that survives review precisely because no behavior is wrong. Record the **pair** now — A56 already got caught once naming a single mechanism for this bug, and the same under-inclusion at the next boundary is the pattern A53/A55 have been tracking all round | reviewer's `plan__…ad5-mechanism-and-severity-verified` §obs; site read by me at `a9afb740` |
| A65 | §2f `AD5` transcription + the B15 follow-up issue text — **the magnitude is "the whole prefill tier minus one, from any starting height", the exposure is unconditional, and the fix is not a pipeline patch** | **The coder built the reviewer's fixture, ran it, and deleted it — so `AD5`'s HEAD behaviour is CONFIRMED BY EXECUTION, and three things about it are worse or more categorical than any reading in this round predicted.** (1) **Magnitude — starting height does not matter.** Prefill lands at **1** from 2, from 4, and from 8, in **both** optimizers, in a **single pass**: an 8-replica prefill tier sheds 7 replicas in one reconcile. So *"sheds prefill to ~1 replica"* reads far too gently — "~1" invites "loses a replica or two", and the measured behaviour is that the tier is emptied but for one, every time. **The cost is not proportional to fleet size; it is the whole tier minus one, whenever the window opens** — the coder's framing, and it is the right one for the issue text. (2) **Role-selectivity is confirmed, not inferred** — decode holds at its starting count at every height in both optimizers, with its demand set to exactly cover its own replicas as a control. That rules out "a global scale-down we mis-attributed to prefill", which was the last benign reading available. (3) **The exposure is unconditional, and this is the sharpest single fact in the round.** `distributeDemandByRole` (`analyzer.go:918-940`) has **exactly two call sites** — `:478` and `:483` — and they are the *sole* source of both demand maps, with `aggregateRoleCapacities`'s own comment confirming it no longer sums per-variant demand. So TA reports prefill `TotalDemand == 0` **for every P/D model, always** — not a data-dependent edge case, not a rare corner. Whenever TA is the only live voter on a P/D model, the drain is available. **Two consequences I own beyond the wording.** First, **the fix cannot be a pipeline patch**, and this is a Type-1 input rather than a scoping detail: both demand terms are decode-rate-denominated *by construction* (the function's own comment says so), so prefill has **no denominator today** — giving it a demand number is a modelling decision about how prefill demand is denominated, which is the designer's and Dean's. That is why B15's follow-up cannot be filed as "add a hold predicate" and be done; the predicate is a *mitigation*, the denomination is the *fix*, and an issue that conflates them will be closed by the cheaper half. Second, **the coder retracted its own earlier mechanism and the retraction stands** — its `plan__…cites-main-not-pr2…` §2 offered a *"missing prefill key → empty ballot → `binder < 0`"* path which **does not fire**: `aggregateRoleCapacities:960-967` gives a P/D model a prefill key, and `analyzer.go:398-408`'s unconditional `PerReplicaCapacity` append means `prcForVariant` returns positive for a live prefill variant. The operative state is the reviewer's: **a real vote, honestly valued zero, with `binder >= 0`** — which is why no abstain-keyed predicate reaches it and why A56's placement conclusion holds. Record the retraction rather than quietly dropping the path: it is the third mechanism proposed for this bug and the second withdrawn, and A56 already carries the pattern | coder's `plan__…confirmed-by-execution-magnitude-is-all-but-one`; reviewer's §3 for the operative state; the two-call-site categoricality is the coder's refinement, confirmed against A63's HEAD trace |
| A66 | §2f `AD5` transcription + the B15 follow-up-issue text — **`AD5`'s second harm is cross-model: the model under-*bids*, so the loss lands on an innocent neighbour, and the understated figure is a hard cap as well as a weight** | **Measured by the coder, located one level up by the reviewer, and one level up again by me.** The measurement: at equal contention the `AD5` shape retains fewer *total* GPUs than a correctly-priced control — not a redistribution between roles, the model loses budget. **⚠ Count correction — the coder and the reviewer both say *"nine of twelve rows"*; the table's own numbers give 8 fewer, 3 *more* (hog 4000 / limit 8 → 5 vs 4; 8000/10 → 3 vs 2; 8000/12 → 5 vs 4) and 1 equal (8000/8, itself labelled crushed).** So *"the model loses budget outright"* is **not general** — the sign reverses at high contention. Record the direction with its exception; the reversal rows are **unexplained, and I am not inventing a mechanism for them.** Mechanism (the reviewer's, verified by me at `a9afb740`): not `distributeGPUsByWeight`, which only divides a grant already won, but the model's **cross-model claim** — `weight_i = priority_i × demand_i` (`rescale.go:47`/`:58`) with `demand_i = modelDemandGPUs`, a **sum over roles** (`:558-566`) into which prefill contributes a structural 0, so the model bids short by its entire prefill role and is outbid by peers that are not misconfigured at all. Mechanism **one level up again (mine)**: that same understated number is also the model's hard ceiling — `capGPUs := demandGPUs` (HEAD `:538`, base `:512`) — so the model is short **twice over**, a smaller weight *and* a lower `CapGPUs`, and the water-fill **re-splits a capped model's freed excess to its peers**. The cap is what explains the measured *pinning*: the `AD5` total sits at 5 across hog demand 1000→4000 at limits 8/10/12, i.e. it never tracks the budget — which a weight-only account does not predict, since weights bind only when the pool is short. **Inherited by construction, independent of any run:** base carries the same weight formula (`:58`), the same role-sum (`:534-537`) and the same `capGPUs := demandGPUs` (`:512`); bug #3 added only the ballot parameter, which cannot lift a structural zero. **Consequences (the reviewer's two, accepted):** a fix repairing only the intra-model role split leaves the lost budget lost — the figure feeding `modelDemandGPUs` has to be right — so `AD5` has **three** sites, not the sizing-plus-gauge pair of A64, because the cap reads that same figure; and the recorded follow-up must say **cross-model**, or it gets mis-scoped as *"prefill stops growing"* — the symptom surfaces on a **neighbour** whose operator has no signal pointing at the misconfigured model, while the losing model cannot show why either (A64's anchor-sourced gauge) | coder's `plan__…contended-path-also-inherited-review-2b-refuted` § *"two things the rows show"*; reviewer's `plan__ad5-second-harm-is-cross-model-not-intra-model-fix-scoping-consequence` (Finding 69, `638a773e`) for the `modelDemandGPUs` level; the `capGPUs := demandGPUs` term, the pinning explanation, the base-side inheritance and the 8 / 3 / 1 recount are mine, verified at `a9afb740` and `075a208e` |
| A67 | §2f `AD5` transcription + the B15 follow-up-issue text — **the only thing standing between `AD5` and a scale-to-zero of a serving role is a model-level guard accidentally handed a per-role singleton, so the obvious cleanup makes `AD5` strictly worse** | **The second fix-scoping trap, and unlike most of this round it is a hazard in the *repair* rather than in the bug.** Mechanism, three-way verified: `scaleDownVariantSet`'s cheapest-at-1 positional clause (HEAD `cost_aware_optimizer.go:157-161`, base `:142`, byte-identical) fires only for the last/cheapest variant with nothing more expensive still holding replicas — a rule written for **model-level** scale-to-zero protection (#1237's intent). **Both callers hand it a role-filtered set** — `cost_aware_optimizer.go:491` and `rescale.go:414`, via `variantsForRole` — so on a P/D model with one variant per role `i == len(sorted)-1` **always** holds and `sorted[:i]` is **always** empty, and the clause therefore fires unconditionally on every role in isolation. The coder confirmed the consequence by **mutation** rather than by reading: disable the clause and prefill goes to **0** on both paths, all six rows, with 4 further specs in 386 also failing. **So the recorded follow-up has to carry two statements that pull in opposite directions, and dropping either one is a defect in the record.** (1) *The guard is misapplied* — true, and it is the natural thing a cleanup PR would notice, because the comment at `:157-158` already describes a **set-relative** rule, so the defect is in the set handed to it and not in the rule. (2) *Repairing that misapplication — giving it the model's variant set, restoring #1237's intent — removes `AD5`'s last brake and enables teardown of a serving role to zero.* It must be preserved deliberately, or replaced in the same change by an **explicit per-role floor**, which is the honest version of what the accident is currently providing. ⚠⚠ **CORRECTION 2026-08-08 — the "explicit per-role floor" branch is not new work, it already exists, and the substitution is provable rather than measured. This softens the row's "last brake" half; it does not retire the row.** `scaleDownVariantSet` reads a per-variant `MinReplicas` and derives `removable := current - minReplicas` under `if removable <= 0 { continue }` (HEAD `cost_aware_optimizer.go:143-152`) — **before** the positional clause, verified by me at the source. The coder measured full substitution at n ∈ {2,4,8} plus a floor of 2, which the accident cannot express; the reviewer then proved it holds for **all** inputs, and the proof is two lines: the clause fires only when `current - n < 1`, i.e. `n >= current` over integers, while `n` is capped at `removable = current - minReplicas` — so it can fire only when **`minReplicas <= 0`**, and any `MinReplicas >= 1` makes it **unreachable**. The converse is the load-bearing half: `minReplicas` defaults to **0** whenever `states` is nil or the field is unset, which is exactly why the accident is today's only brake. **Three qualifications for the filed text, and the first two are why this is a softening rather than a retirement.** (1) The repair removes the last **automatic** brake — the exposed set is every P/D model whose prefill variants carry **no explicit floor**, which is not "every model" and is not "none". (2) `MinReplicas` is an **operator-set** per-variant field, not something the optimizer synthesizes, so *"replaced by an explicit floor"* resolves into two options with different coverage: telling operators to set it works today with no code but leaves unconfigured models exposed, versus having the code synthesize a floor for a role whose demand nobody priced, which is new behavior and a Type-1 question. **The reviewer's Finding 71 §2 point is the one that decides between them and it is not a scoping detail:** `AD5`'s own premise is a role whose demand nobody priced — a model already in this state *because* configuration is absent — so the documentation option asks for correct **additional** configuration from precisely that operator, and its failure mode is **correlated** with the bug it is meant to contain. It must not be scored as equivalent coverage if it enters the deferral record as the mitigation. (3) Mine, and it bounds the value of the cheap option further: a floor of **1 changes no outcome here** — the shed in this regime is n → 1, exactly what the accident already yields — so the lever only bites at **≥ 2**. What a floor of 1 does buy is independence from the positional accident, which is a robustness argument, not a protection argument. **One precision on the row's own claim, also mine:** the positional clause protects the **last (cheapest)** variant of a cost-**descending** set (`cost_aware_optimizer.go:120`), so on a P/D model with **two or more prefill variants** the more expensive ones are shed first and are not protected at all — an individual prefill variant can reach **0 today**, with the role total surviving at ≥ 1 only because something cheaper still holds. So the "single surviving replica" is a **role**-level statement about single-variant roles, and the no-scale-to-zero-as-current-behavior constraint below is about the **role**, not about a variant. **Two wording constraints follow, both for the filed text.** First, **do not describe a scale-to-zero as current behavior** — it is what the mutation shows, not what ships; the accurate form is *"the role is drained to a single replica, and the only mechanism preventing zero is a guard whose applicability here is accidental."* Second, the filed text is a public artifact under the no-plans-tokens rule, so this lands as prose about the cheapest-at-1 protection and the role-filtered caller sets, with no `AD5`/`AD7`/`Fnn` tokens | the mutation is the coder's `plan__…finding68-experiment-run-vg-up-does-open-reachability` §4; the `variantsForRole` singleton argument and the *"defect is in the set, not the rule"* reading are the reviewer's Finding 70 §3 (`e38e7d22`); the base-side identity of the clause and the public-artifact wording constraint are mine |

## B. Conditional — each waits on one decision

| # | Depends on | Change |
|---|---|---|
| B1 | **(vi) claim pricing** | Land the chosen disposition: accept-and-document / headroom partial / **option (d)** (`min(gpusPR / PRC)` over feasible candidates). If (d): record that its neutrality is **contingent** (via `ceil` + a binding bottleneck), *not* structural — golden scenario A has equal `GPUsPerReplica`, unequal PRC, and the reference **flips** `cheap → expensive`, halving the claim `0.5 → 0.25` GPUs. Also record that (d) changes the **ranking key** for any unequal-PRC role, which no golden covers |
| B2 | **(vii) Finding 28** | Add a discriminating spec for `fairShareRolePick`'s per-role budget. Reviewer's table: clamp-only passes **both** shipped specs, so `committed0`, `reserved`, the per-draw holdback and `firstDraw` are pinned by nothing. §C6e asked for "roles that would each individually fit but jointly overrun"; the shipped fixture has both roles individually exceeding `target`, which is what lets clamp-only pass. Technique already established by `34b18bc5` (call the returned pick closure directly). This is **Finding 20's shape recurring** |
| B3 | **(viii) Finding 33** | A step naming the four shipped §4a defects — `greedy_score_optimizer_test.go:1602-1603` (handoff **path**), `:1741-1743` (three handoff filenames, one written today), `:1604` ("open with the Type-1 owner" — a mis-routing I introduced, now in code), `:1736` (`784c2b5c`, a pre-rebase SHA). My recommendation: **not** C9 — the handoff refs are `.DONE`+`git rm`-ed before the PR merges, so they are dead on arrival |
| B4 | **(ii) C6e item 2** | If fixed: a **C6g** row in §0 between C6f and C11, and the git-order line updated. Rationale for a separate commit: it is entitlement accounting, not sentinel ranking, and C11 is already a behavior-change commit whose golden attribution should not absorb a second change |
| B5 | **(i) C6c fork** | Whichever of (a) restore `ceil` / (b) defer-not-evict / (c) `max(1, floor(x))` is chosen, applied as a **three-site** policy per A16 — not a single-expression edit. **⚠ Two of the three options have collapsed, 2026-08-08 — the fork is narrower than when this row was written.** **(b) is already shipped**, from the other side: C6e's `firstDraw && capN < 1 ⇒ capN = 1` *is* defer-not-evict for a model's first grant, which is the case the C6c evictions came from — the coder's own reading of this ("my C6c option (b) … is answered from the other side and is already in the branch") is correct and I verified the guard's provenance (absent at `34b18bc5`, introduced by `784c2b5c`). **(c) `max(1, floor(x))` is now close to redundant** — it is (b) applied unconditionally instead of on the first draw, and A55's measurement shows no eviction rows remain to justify the unconditional form; it would also re-introduce the Σ-entitlement overshoot (B8) at every draw, which is the specific harm `floor` is being chosen to avoid. **So the live fork is (a) `ceil` vs plain `floor`, with (b) already in-tree underneath either** — and A55/B10 recommend plain `floor`. The **three-site** application requirement per A16 stands unchanged and is the part of this row that still has teeth: the resolution is a policy across the granting sites plus A55's six-block prose sweep, not an edit to `:837` |

| ~~B6~~ | ~~(xii) Finding 47~~ | **CLOSED — no decision needed; the coder landed it voluntarily as `79a590d6`** *"pipeline: test the admission ceiling at fillRole (C11 D-b follow-up)"*, **+73 lines** to `rescale_test.go` (not the ~15 I estimated). So the question this row was waiting on — C11's own row vs a `(D-a)` follow-up — was answered in the tree, as C11's: the message labels it a `D-b` follow-up, which is the shipped half. Nothing to apply. The two §4a/`max`-shadowing items above (A29/A30) are findings **against this same commit**, so it is not defect-free, only complete |
| B8 | **(xiv) `replicasToCover` Σ-overshoot** | The `ceil` ships with a **known, coder-acknowledged consequence recorded only in a test comment**: `greedy_score_optimizer_test.go:1428-1430` — *"without that, Σ_role spend exceeds target by the round-up, which is the **deferred `replicasToCover` item** and not this one"* — and the C6e fixture is built from whole multiples specifically so it cannot trigger. Whatever Dean decides on the rounding, this deferral needs a **plan home and a §4b classification**; right now it exists nowhere but a comment inside a test that is designed not to exercise it. Independently of the rounding decision, this row should land |
| B9 | **(xv) §4a sub-class ruling** | Does §4a cover **bare "the plan"** and **review-finding-number attributions**? `79a590d6`'s message ends *"Raised by the PR-2 internal review as Finding 47."*; C10's has *"the plan has `resolveKSat` type-assert …"* and *"Two deviations from the plan"*. Neither carries a path or filename, so both sit **outside §4a's literal prohibition** — this is a convention extension, not an existing violation. Two reasons it needs deciding rather than drifting: a finding number *reads as a precise citation* yet is unresolvable for anyone reading `main`, arguably worse than the vaguer "the plan"; and the coder is **now doing this regularly**, which is otherwise a good habit worth preserving in some other form. The reviewer's read (which I share): the substance is always already in the message, so dropping the identifier is free. **The answer changes C9's scope from 54 locations to 54 plus a commit-message pass**, which is why it should be settled before C9 rather than after | reviewer §4, explicitly framed as a convention call and not a review finding |
| B13 | **(xix) §4b admits only two labels; C9d used a reasoned third** | **Mine. A convention question, not a coder defect — and I would rule for the coder.** CODER-CONVENTIONS §4b requires every removal be classified **DEPRECATED** or **DEFERRED**. `4e369f10` explicitly declines both: *"a considered removal of superseded coverage, not a deprecation and not a deferral: nothing about these scenarios is postponed, they are asserted in a strictly stronger place."* The **substance** the convention is protecting is fully delivered — the "state why" DEPRECATED demands is there at length, plus a per-spec mapping to the replacement, which is more than either label asks for. But the **label** is outside the vocabulary, so a future reader grepping the ledger for the two sanctioned words finds neither. Two possible resolutions and they are not equivalent: (a) file it under DEPRECATED with the coder's prose intact — cheapest, but "deprecated" connotes *functionality lost*, and here coverage strictly **increased** (the plumbing the removed smoke test existed to prove is now exercised by twenty-one specs), so the label actively misdescribes it; (b) admit a third class — **SUPERSEDED** = "removed because a strictly stronger assertion now covers it, mapping recorded" — which is what actually happened here and, I expect, what happens to every characterization suite once its refactor lands. **My recommendation is (b)**, because this will recur: a characterization suite is *designed* to be temporary, and forcing its retirement through a vocabulary built for lost functionality is what produced the coder's three-paragraph justification of why neither word fits. **Routing note — this is Dean's, not mine:** §4b lives in CODER-CONVENTIONS, and "who edits CONVENTIONS.md" is already an open governance question in `governance-follow-ups.md`. I am not editing it; the Type-3 §12 ledger row is mine either way and I will write whichever label he picks | mine, from `4e369f10` vs CODER-CONVENTIONS §4b |
| B12 | **(xviii) Specify the §4a sweep as a grep-to-zero gate, not a count** | **Mine; it falls straight out of A43 + A44 and it retires a recurring churn rather than adding work.** Three different §4a totals now sit in three documents — plan `:1647`'s 32, A10's ~49, Finding 59's 35 — and A44 shows they disagree partly because they measure different token sets and partly because **the number grows monotonically with every test commit**: `T1-ols` alone went 19 → 36 across C9b/C9c, and 24 of the 36 sit in two test files that later commits keep extending. Any integer written into the plan is stale on the next commit, and each restatement has cost a verification round. **So the C9 sweep row should specify a gate, not a target:** *sweep until `git grep -nE '<enumerated pattern>'` returns empty*, with the pattern spelled out in the plan and covering **both** A43 shapes — (i) the token families, (ii) plans-doc filename/path citations — and with the two known **carve-outs named inline** so the gate is achievable: the pre-existing `throughput-analyzer.md:714` main-side citation (inherited, tracked elsewhere), and whatever Dean rules on the golden-scenario names `A1`–`A4`/`B1`/`B2`/`C1`/`M1`–`M7`, which are **test identifiers, not plans-branch tokens** and would otherwise make a naive pattern unsatisfiable. That last point is why this is conditional rather than unconditional: **the gate cannot be written until the §4a carve-out ruling lands** (the same ruling B9 waits on). Note it does **not** replace the prose review A40 argues for on the `ceil` sites — a grep gate is the right tool for tokens and the wrong tool for a rounding argument; both belong in C9, separately scoped | mine, from A43 + A44 |
| B11 | **(xvii) C6f — abstention is not a liveness guarantee** | **Mine; neither the designer nor the reviewer raised it, and it is a direct consequence of the designer's own structural lesson.** Its §2 explains why map §9 missed the use-case failure: §9's verdicts answer a **consistency** predicate ("is this sized from the binder's own metric?") not a **liveness** one ("does the variant come up at all?"), and **`N8` compounds it — N8 *prefers* abstention (`PRC = 0`) over borrowing, so inside the design's own value system a cold variant landing at `PRC = 0` reads as the *correct* outcome rather than as a break.** C6f is the commit that codifies exactly that preference (`W4`, abstain-when-unpriced). **This is not an argument against C6f** — abstaining still beats voting zero, which asserts "no replicas needed," and A41 shows abstention is precisely what makes `[sat, TA]` non-P/D work. The consequence is narrower and it is a documentation duty I own: **C6f's plan text and its dev-guide counterpart must state that abstention is a correctness guarantee about the *vote*, not a liveness guarantee about the *variant*** — an all-abstain role is indistinguishable from a satisfied one at the combine, and the residual is A35's `[TA]`-only cell. Without that sentence, the next reader of C6f re-derives "PRC = 0 is fine" as a general principle, which is the exact inference that produced the retracted "nothing is broken." **Decision for Dean:** whether this lands as prose in C6f (my recommendation — it is two sentences at the site that creates the hazard) or is deferred to C9 with `W3`/`U5` | designer's per-config verdict handoff §2 structural lesson; the C6f consequence is mine |
| B10 | **(xvi) T1-1 upgraded — the two test comments are in tension, and resolving it makes the fork a live invariant violation** | **Mine, not the reviewer's; it upgrades my own A37 assessment and it is the strongest argument yet for the authority's direction.** The C6c fixture's leading comment claims materiality is **iterations, not replicas** — *"at Optimize() level an understated cap costs iterations rather than replicas, because the allocation total is bounded elsewhere and each iteration re-picks."* But the model-level entitlement test 25 lines later says the opposite about the same `ceil`: *"replicasToCover rounds nothing up: **without that, Σ_role spend exceeds target by the round-up**, which is the deferred replicasToCover item and not this one."* Both are the coder's own words. **The resolution is that "bounded elsewhere" means bounded by *availability*, while the bound the round-up escapes is the *entitlement*** (`Σ_role spend ≤ target`, in GPUs — the invariant the whole fair-share mechanism exists to enforce, and the one `W1`/C6e is about). Under that reading both comments are individually defensible and the net is: `ceil` cannot exceed physical capacity but **does** exceed the fair-share entitlement. Three consequences. (1) The Type 3's *"one-replica behavior change"* framing is **correct after all** — against fair-share, not against availability — so that language stands rather than being overstated. (2) The fixture comment's *"iterations rather than replicas"* is **misleading as written**, true only against the bound that was never in question. (3) **The invariant test is constructed to dodge the violating region** — its topology is chosen so *"replicasToCover rounds nothing up"* — which is A39 row 3 one level up: the mandated fixture freezes the wrong direction, and the invariant test that would catch the consequence is shaped so it cannot appear. **So T1-1 is not merely doc-vs-code with the safe side compiled (my A37 wording, now too weak): the code side violates a stated invariant and neither test can surface it.** That is a correctness argument for `floor`, not a consistency one. **Not traced by me, and not by the reviewer either:** whether the overshoot is bounded to one replica per role per boundary (`W1`'s decrement plus `!allocated ⇒ remaining = -1` may bound it; `:831` claims the water-level check stops a second). The tension and its resolution stand regardless of that bound. Cross-refs B7, B8 — B8 already recorded the overshoot as deferred; what is new is that it is the *same* effect T1-1 is about, which is why the two cannot be dispositioned independently. **⚠ RECOMMENDATION REVERSED 2026-08-08 — I now recommend `ceil` (option (a)), and I was wrong to argue for `floor`.** The coder implemented the plan's `floor` exactly as specified, measured it, and backed it out: **9 failures of 334**, deltas of **more than one replica** (`3→1`, `2+→1` collapses) including **`bv` 6→2 under an *unconstrained* GPU budget** — far outside the plan's own stop condition of "one replica at a mid-replica boundary." Restoring `ceil` left **exactly one** red test, T1.4, which §2d.6 already schedules for rewrite, so the 9 decompose cleanly into 8 rounding effects + 1 piece of planned work. That is the disproof the plan's stop condition asked for, and it is measurement, not argument. **The mechanism, and the asymmetry that decides it — mine, and it is the piece my B10 was missing:** `fairShareCap` is a `min` of **two terms answering two different questions** — the entitlement (*how many replicas does this gap need?*) and the real pool `gpusAvail/gpusPR` (*how many can the budget afford?*). Flooring the **pool** term is right and already the case; flooring the **entitlement** term is what breaks, because `capN == 0` reaches `!allocated ⇒ w.remaining = -1`, which drops the model out of `active` **permanently** — so it is a *termination*, not a smaller allocation. And the two zeros are not equivalent: a zero from the **pool** term is safe, because the pool only ever *shrinks* within a cycle, so eviction ≡ deferral; a zero from the **entitlement** term is unsafe, because the entitlement **grows** as the mean falls in later rounds, so the evicted model loses allocations it would have earned. `ceil` confines zero to the pool term. That is why `ceil` is correct here and not merely cheaper. **It also aligns with Dean's standing most-important-point** — `ceil` *is* current behavior; `floor` changes it in 8 places, several by more than one replica. **What the tree ships now is stronger than either fork side**, verified by me at `a9afb740`: `replicasToCover` = `ceil` (`:833-838`), the pool term floored, a `firstDraw && capN < 1 ⇒ capN = 1` indivisible-unit grant (`:702-711`) that closes even the pool-term eviction before the model's first commit, a per-role GPU holdback, and a `continue`-not-fall-through at the `maxTarget` clamp so an exhausted per-variant ceiling skips the variant instead of abandoning the role. The per-term rule is stated in a shipped comment at `:695-700`. **So my (1)/(2)/(3) above are superseded:** the *"one-replica behavior change"* framing was wrong in magnitude (it is up to 4), the fixture comment's *"iterations rather than replicas"* is defensible under `ceil`, and the invariant test's topology is not dodging a violation but matching the shipped rule. **The Σ-entitlement overshoot (B8) remains real and remains deferred** — that is the one part of B10 that survives, and it is now clearly the *separate* item B8 always said it was, not the same effect as T1-1. **Consequence for Dean's queue: T1-1, B5, B7 and B10 collapse into one decision with one recommended answer** — confirm `ceil`, strike §6's C6c *"the `ceil` must be gone"* bullet, and replace it with the per-term rule so the fork cannot reopen. The **Type 1 is the side that must move** (`:1159-1160`, `:1163-1168` mandate `floor`); that is the designer's, not mine. **⚠⚠ RECOMMENDATION RESOLVED 2026-08-08 — I now recommend `floor`, i.e. the frozen Type 1 as written, and my `ceil` reversal above is withdrawn. See A55 for the measurement.** The honest arc, stated so it reads as evidence-tracking rather than vacillation: the Type 1 and the plan both specified `floor`; I departed from the authority on the strength of a *measurement* (9 failures, −4, collapse-to-1); review showed that measurement predated its own mitigation (A54); the refreshed measurement at HEAD shows **5 failures of 386, worst delta −2, zero collapse or eviction rows**, with **both** of the C6c-era worst rows (`optimizer_equivalence_test.go:135-137` `bv`=6, `greedy_score_optimizer_test.go:2148-2157` `team-b`) present at HEAD and **green under `floor`** — so the evidence that justified departing from the authority has been withdrawn, and the correct move is to return to it, not to hold a position the evidence no longer supports. **What survives from this row unchanged:** the two-comment tension and its resolution (the round-up escapes the *entitlement* bound, not the *availability* bound); the pool-vs-entitlement asymmetry as a *description* of the mechanism; and B8's Σ-entitlement overshoot as a real, separate, deferred item. **What is withdrawn:** the inference from that asymmetry to `ceil`. The asymmetry is real but its damaging consequence — `capN == 0 ⇒ !allocated ⇒ w.remaining = -1`, permanent eviction — is what C6e's `firstDraw` guard closes, and the refreshed measurement confirms behaviorally that no eviction row remains. An asymmetry whose harmful branch is guarded is a design note, not a fork argument. **Also withdrawn: the "current behavior" appeal.** `ceil` is the *branch's* current behavior, introduced by this very PR's C6c hold; it is not `main`'s behavior, so Dean's most-important-point (don't break current behavior when sat-only) does not adjudicate this fork in `ceil`'s favor — and the sat-only goldens are the direct test of that point, which C9d's removal and the suite churn now bear on separately | coder's measured fork handoff §1 and the HEAD re-measurement; shipped shape, the pool-vs-entitlement asymmetry, and the withdrawal all verified and derived by me at `a9afb740` |
| B7 | **(i) restated — T1-1 / AM-1** | The fork is **already resolved in-tree as (a)**: `replicasToCover:833-838` ships `ceil` with a written rationale at `:824-832`, and there is no `math.Floor` in non-test `greedy_score_optimizer.go`. So the row is no longer "apply one of three" but a **doc-vs-code divergence**: frozen Type 1 `:1159-1160` mandates `floor`, the tree ships `ceil`. Exactly one must move, and if the Type 1 moves this row becomes a no-op. **⚠ RESOLVED 2026-08-08 — the recommendation is that the *code* moves to `floor`, per A55's re-measurement, which makes the Type 1 correct as frozen.** Beyond the measurement there is a **governance-cost asymmetry that points the same way and is worth one line in front of Dean:** resolving to `floor` needs **no** post-freeze Type-1 amendment — the authority already says `floor`, so the change is entirely within my Type 3 and the coder's C6c. Resolving to `ceil` needs an amendment to a `Status: FINAL` document frozen at `8c2a9b04`, which is the designer's to write and Dean's to route, and it would be the first post-freeze change to it. So `ceil` costs a measurement *and* a freeze exception; `floor` costs neither. That asymmetry does not decide a correctness question on its own, but where the measurement has stopped arguing for `ceil` it is a clean tie-break rather than a thumb on the scale. **⚠ One honest qualification to that, and it cuts against me — Dean should have it in the same breath as the recommendation.** The **designer has praised the `ceil` rationale on the record**, having read the shipped code: *"the rationale is written at `:826-836` and is a good one — the water-level-gap-not-a-pool distinction is exactly the right axis"* (`plan__ta-anchor-designer-withdraws-t1-1-and-the-coder-accusation.md`, verified by the designer at `2ae440e3`). The same handoff withdraws T1-1's urgency and its C6c scheduling instruction but **does not** withdraw `floor` from the Type 1, and states plainly that one of the two must move and **it is Dean's call**. So I must not present `floor` as the unambiguous position of the authority: the authority's *text* says `floor`, its *owner* has called the `ceil` reasoning right on its axis, and the owner has explicitly declined to resolve it. My recommendation stands on the **measurement** — 5 of 386, zero evictions, four real specs at the plan's own stop condition — and on the fact that the eviction hazard which was the strongest `ceil` argument is now guarded. It does **not** stand on "the authority already says so", and the governance-cost line above should be read as a tie-break available *if* Dean finds the measurement decisive, not as an argument that the question is already settled |
| B14 | **(xx) the `both`-shape publication split — a Type 1 question, and the coder was right to refuse it** | **Not mine and not the coder's: it is a design question about what the CRD publishes, and it is now frozen into two goldens.** The coder named it and declined to decide — *"a Type 1 question … I am refusing to decide it"* — which is exactly the *"don't leave design decisions to coder"* rule working as intended, and it should be recorded as compliance, not as an open defect against C9. **The two instances, both now golden-frozen:** M1 publishes saturation's `RequiredCapacity` of 15000 **beside a replica count driven by throughput's 40000** — so a reader of the status sees a capacity figure that did not size the decision; M2 publishes saturation's `SpareCapacity` of 30000 **on a decision forbidden to spend any of it** — a headroom figure that is real per-analyzer and unusable in aggregate. Neither is a wrong *decision*; both are a wrong *story about* the decision, which is the same failure character as the `OptimizationReady=True`-with-no-event bug and the halved-decode telemetry in A38: the system acts correctly and the published state misdescribes why. **Routing: designer, then Dean — I am not folding it into the Type 3**, because whatever it resolves to changes what the multi-vote goldens should assert, and those goldens are the ship gate. **Consequence if it resolves against the current shape:** M1/M2 are characterization tests, so the fix moves goldens the coder deliberately froze — cheap now, expensive after the PR opens, which puts it in the same time-boxed class as the reword window. Flagging the timing, not asking for a decision here | coder's close-out §1, declining to decide; routing is mine |
| B15 | **(xxi) `AD5` scope — hold predicate in PR-2, or a recorded known limitation?** | The designer hands this to me explicitly (*"Your call, and both answers are defensible: in-scope for PR-2, or recorded as a known limitation of `[sat,TA]` under sat outage"*), with its own recommendation being *"a predicate and not a new mechanism: when the binding analyzer is not role-complete for a role, hold that role — the same per-model hold already specified for `bindingAnchor == nil`"*, and its stated worry being *"what I would not want is `VG-up` shipping with nobody having noticed it unmasks this."* **Two facts change the shape of the call. (1) `VG-up` already shipped** — `952d2fff`, 20 commits before code-complete, `analyzer_helpers.go:332-340` — so this is not "catch it before it lands" but "a new commit on `a9afb740` versus a follow-up PR", and nobody-noticed is no longer the risk since it is now noticed and recorded (A56/A57). **(2) The predicate cannot go where `AD5` points** (A56): the anchor's `RoleCapacities` no longer sizes a role, so the site is where the ballot is built or where `binder < 0` is handled — a placement decision that does not exist in any document yet, and per A57 §4 it also needs a sub-decision on *which state triggers the hold* (nobody priced this role vs the analyzers that model it agree on zero — different states, both returning 0 GPUs today). **⚠ REVISED after A63 — the recommendation is unchanged but its basis is different, and the severity it was weighed against was understated.** What I first offered rested on the window being narrow and A57 holding the cancellation in place. Both of those moved: the consequence is an **active drain of prefill to its floor while decode scales normally** (worse than the freeze I described), and A57 turns out to cover only the from-zero corner. **What replaces them is stronger: A63 establishes the drain reproduces at base `075a208e` unchanged** — base already bound TA under a non-live sat, already read prefill demand 0 off that anchor, already gave prefill weight 0, already called `reclaimRole` — so `AD5` is a **pre-existing defect of the opt-in TA path, not a PR-2 regression**, and PR-2's ballot argument if anything gives prefill demand a second chance to be non-zero. **My recommendation: still defer the predicate to a follow-up**, now on attribution rather than on rarity — asking a code-complete 25-commit branch to fix an inherited bug, via a placement that exists in no document (A56) and needs a trigger-state sub-decision (A57 §4), is scope creep on someone else's defect, and under *"don't leave design decisions to coder"* it would block the branch on new Type-1 work. **What the deferral now requires instead of A57: a filed follow-up, because "inherited" is only an acceptable answer if the bug is actually recorded somewhere that outlives this round** — issue text should carry A63's mechanism chain, A64's second observability site, and the reviewer's fixture. A57 stays worth landing but is **no longer the condition of the deferral**. ⚠ **UPDATED again 2026-08-08 — the coder ran the fixture, so the severity the deferral is being weighed against is now measured rather than argued, and it is larger; the recommendation still does not move, and I want to be explicit that measuring it did not change my answer.** Per A65: prefill lands at **1 from any starting height** in a single pass (8 → 1), decode holds as a control, and the exposure is **unconditional for every P/D model** because `distributeDemandByRole` has only two call sites and excludes prefill by construction. Deferring a *confirmed, unconditional, whole-tier* drain is a strictly harder call than deferring a suspected narrow one, and it is still the right one **only because A63's attribution holds** — this is inherited, PR-2 authored neither path, and the two paths' inheritance chains are now separately verified (Path A confirmed-by-execution at HEAD with base's gate checked; Path B by reading). If the attribution were wrong, the magnitude would flip this row. **One thing the measurement does change: what the follow-up must say.** A65's denomination point means the issue cannot be filed as "add a hold predicate" — the predicate is a *mitigation* of the symptom, while the *fix* is a modelling decision about how prefill demand is denominated (designer's and Dean's, per plan §7.1 and the coder's explicit refusal to decide it). An issue conflating the two gets closed by the cheaper half, leaving every P/D model still structurally reporting prefill demand 0. So the filed text needs **both halves named and separated**. **Also worth offering, not directing: the one cheap experiment that would close attribution outright** is running the same fixture at base `075a208e` — my base claim is a five-function reading, and the coder's harness for it existed hours ago. That is a coder action in the coder's tree and I am not requesting it via a trigger; it is Dean's to ask for if he wants the authorship question settled by execution rather than by source. ⚠⚠ **BASIS MOVED A SECOND TIME, and this one weakens the row — Dean should have it stated plainly rather than buried.** Per A63's dispatch correction, **Path A — the *executed* path — is inherited only conditionally**: for a saturation that dies holding any positive `RC` on any role (decode alone suffices), base diverted to the scale-up branch and did **not** drain, so PR-2 converts base's *"scales up on stale data"* into *"sheds prefill to one replica."* **"This is not PR-2's bug" therefore no longer carries the row on its own** — it is true unconditionally only for Path B, which nobody has executed. **What the defer now actually rests on, in order of weight:** (1) **the fix is a modelling decision, not a patch** — prefill demand has no denominator today (A65), so the real fix is the designer's and Dean's, and per *"don't leave design decisions to coder"* a hold predicate is a *mitigation* the branch cannot correctly scope; (2) the placement for even that mitigation exists in **no document** (A56) and needs a trigger-state sub-decision (A57 §4); (3) Path B is unconditionally inherited, and Path A's machinery is inherited with only its reachability changed. (1) is now load-bearing and (3) is support — the reverse of how this row read an hour ago. **Two things I will not shade:** base's diverting behaviour is *itself* a bug, so "no drain at base" is not "base was fine" — it is bug-masking-bug with the mask in the shipped base; and against a *confirmed, unconditional-per-model, whole-tier* drain (A65), a conditional-inheritance defence is thinner than the one I offered when I recommended deferring. **I still recommend defer, on (1), and I flag that this is now a genuinely close call rather than a clear one.** The reviewer's two-variant base experiment would settle which side of it we are on and costs one run of a harness that existed hours ago. ⚠⚠⚠ **BASIS RESTORED 2026-08-08, and this is the last movement on this row — the close-call flag above is withdrawn.** Per A63 pass three: the coder ran the fixture at base and it drained **identically** (prefill 1 from 2/4/8, both optimizers), and — separately, and this is what actually settles it — the dispatch's diverting condition requires an analyzer to report NoData/Error on **every** variant while aggregating a positive role `RC`, which is precisely the *"dead ⇒ `RC = 0`"* invariant that Dean's own PR-1 `D1`/`D2` deferral already records as **emergent, not enforced, with a named future-analyzer trigger**. So the reviewer's finding is real but is **the other half of a hazard this branch already tracks**, unreachable for the shipped analyzer set — it does not make `AD5` PR-2's authorship. **Ordering of the basis, final form:** (1) **the fix is a modelling decision, not a patch** (A65 — prefill has no denominator; designer's and Dean's, and under *"don't leave design decisions to coder"* the branch cannot correctly scope it) — still primary; (2) **the attribution is inherited for every dead-analyzer shape the current analyzer set can produce**, with the residual conditionality folded into `D1`/`D2`'s existing trigger rather than standing as an open unknown — restored to real weight, where an hour ago it was carrying almost none; (3) placement exists in no document (A56) and needs a trigger-state sub-decision (A57 §4). **Two honesty notes that survive the restoration:** base's diverting behaviour, where it occurs, is *itself* a bug, so this remains bug-masking-bug rather than "base was fine"; and A63's residual link (a NoData/Error variant cannot carry positive `RequiredCapacity`) is **emergent-and-unverified**, so (2) rests on an unproven-but-cheap-to-prove step. **Recommendation unchanged throughout all three passes: defer, on (1).** Worth noting for the record that the recommendation never moved while its basis moved three times in both directions — measured severity did not flip it, and neither did the dispatch finding. ⚠⚠⚠⚠ **FOURTH MOVEMENT, same day, and it changes only the support: per A63 pass four, both drain paths floor at 1 via the same helper, so *which path either run executed is unknown* and every "the executed path is A" phrasing in this row is struck.** Basis **(2)** accordingly rests on A63's **(b)/(c) production-reachability trace alone** — a dead entry cannot carry positive role `RC` unless an analyzer reports NoData/Error on every variant while aggregating a positive requirement, which is the already-tracked `D1`/`D2` invariant — and no longer on path identity, which was never load-bearing for it. Basis **(1)** unchanged and still primary. **Two smaller consequences for the filed text:** my own *"reclaims to 0"* prediction was wrong by the same omission (the floor is 1, not 0 — the call covers the whole allocation, the helper caps the outcome), so the issue must not claim a scale-to-zero; and the cheapest-at-1 positional rule is what stands between `AD5` and actually emptying a serving role, an accidental protection worth naming so nobody removes it as dead weight. Severity otherwise unchanged — 8 → 1 sheds 7. **Recommendation unchanged across all four passes: defer, on (1).** Escalated to Dean, not applied; filing is his call. ⚠⚠⚠⚠⚠ **FIFTH MOVEMENT 2026-08-08 — and this is the one where the recommendation itself moves. Basis (2) is now REGIME-SCOPED, the `deadRC > 0` half is PR-2-authored and PRODUCTION-REACHABLE, and I no longer recommend an unconditional defer. Read this movement in place of the four above where they conflict.** **What changed.** Per A63 pass six: the reviewer's refutation experiment ran, and with the dead entry carrying a positive per-role `RequiredCapacity` **base preserves prefill and HEAD drains it to 1**. So `AD5` splits by regime — `deadRC = 0` inherited (base ≡ HEAD, twelve contended configurations plus three steady heights, both optimizers), `deadRC > 0` **HEAD-only, authored by `952d2fff`**. And pass three (c)'s claim that the diverting regime is *"unreachable with the shipped analyzer set"* — the load-bearing step of basis (2) — is **withdrawn**: I traced the reachable state myself and it needs no future analyzer, because per-role `RequiredCapacity` is written by the engine's universal threshold post-step from per-role **demand**, while informativeness keys only on per-**variant** `Reason`. The two are computed from different inputs, so *"every variant NoData"* and *"positive role `RC`"* are independent, not exclusive. **The reachable state, and why I am not calling it a corner.** Four conditions: a P/D model; every variant `NoData` (no live metrics instances **and** no store record); positive **scheduler-queue** demand on some role (the only surviving demand term once variants go blind); and a live fleet in scale-target state to drain (preserved — `readyCount` is spec/status-derived, not metric-derived). **One event supplies two of them at once:** both `lastGoodAnalysis` and the capacity store are in-process maps, so a **controller restart during a metrics outage** gives a cold store *and* `Live = false` on cycle one, with no staleness window. The operational shape is therefore: *metrics blind, requests queueing, controller restarts — and PR-2 sheds the prefill tier to one replica*, with the prefill gauge reading 0 for an unrelated reason (A64). Frequency is unquantified and I will not pretend otherwise, but "unreachable" is off the table. **A reframe that cuts against my own earlier defence of this deferral:** the coder's *"base scales up on a dead analyzer's stale figures"* is wrong in the reachable instance — the positive `RC` is **derived this cycle from live queue demand**, not carried forward (no result carry-forward mechanism exists). Base is acting on **half-fresh** data — real demand, absent capacity — which is a materially more defensible thing for base to be doing than the phrase "stale figures" suggests, and correspondingly makes HEAD's drain the harder of the two states to justify. The bug-masking-bug framing survives, but the two bugs are **not** comparably bad in that reconcile. **Basis, current form.** (1) **the fix is a modelling decision, not a patch** — unchanged, primary, and now carrying the row essentially alone; (3) placement for a mitigation exists in no document (A56) and needs a trigger-state sub-decision (A57 §4) — unchanged; (2) **carved**: inherited for `deadRC = 0`, PR-2-authored for `deadRC > 0`. The reviewer asked for the carve-out to be stated rather than left in the unconditional form; agreed, and it is stated. **A third option neither the designer nor I had on the table, which is what actually moves my recommendation — stated as an option with its cost, deliberately NOT adopted.** The designer's hold predicate is expensive because it needs prefill demand to mean something. But the `deadRC > 0` regression has a **much narrower** shape: it exists because `VG-up` removes an entry that was, however accidentally, the only thing holding the model off the scale-down branch. A gate scoped to *that* — when `votingResults` prunes a non-`Live` entry that carried positive per-role `RC`, hold the model's scale-down for this cycle — needs **no** prefill denomination, sits inside PR-2's own commit rather than in new Type-1 territory, and is **asymmetric in the useful direction: it keeps `VG-up`'s benefit (do not scale *up* on a dead analyzer) while restoring base's protection (do not scale *down* merely because a dead analyzer's vote vanished).** Costs, stated so the option is not oversold: it is still new behavior on a code-complete branch; it has its own trigger-state question (hold scale-down only, or hold the model?); and the test must be built in the non-informative-with-demand shape, because **a stale-timestamp test lands in the inherited regime and passes on both sides** (A63 pass six trap one). **Recommendation, changed.** I no longer recommend an unconditional defer, and I am not going to launder that as "unchanged". **Split the row:** the *fix* — prefill demand denomination (A65), the cross-model bid and its cap (A66), the observability pair (A64) — stays deferred to a follow-up on basis (1), which nothing in this pass touched. The `deadRC > 0` **regression** is a different question, and it is PR-2's own: **if Dean is willing to spend one commit plus one test, the narrow asymmetric gate is what I would take**, because it removes a production-reachable, PR-2-authored drain of a serving role at a small fraction of the modelling cost. If he is not, defer remains defensible — but then the deferral must be recorded **with the carve-out**, not in the "this is not PR-2's bug" form, because that form is now false for the regime that matters. **Two notes that reach past this row.** First, **Dean's PR-1 `D1`/`D2` deferral rationale contains a false premise** — its named trigger was *"a **future** analyzer that carries forward stale-but-informative `RC > 0` with an aged `AnalyzedAt`"*, and the actual break is the **shipped** saturation analyzer via the engine post-step, with no aged timestamp involved. This does **not** create a PR-1 problem: PR-1 shipped the `Enabled`-only prune, i.e. the base behavior that *preserves* prefill, so the consequence lands entirely on PR-2's `VG-up`. It does mean the recorded rationale should not be cited forward as-is. Second, **the filed follow-up text now has six requirements**, all of which have been got wrong at least once in this round: name both halves (mitigation vs denomination); say **cross-model**, not intra-model; name **three** fix sites, not two (sizing, gauge, and the demand figure the cross-model cap also reads); record the **regime split**; do **not** assert a scale-to-zero as current behavior while recording that the only brake is an accidentally-applicable guard whose obvious cleanup removes it (A67); and do not repeat the *"nine of twelve rows"* count (A66 — it is 8 fewer, 3 more, 1 equal). **Escalated to Dean, not applied. Nothing in this row is a coder instruction and no trigger accompanies it.** ⚠⚠⚠⚠⚠⚠ **SIXTH MOVEMENT, same day, arriving after the fifth was written — appended rather than folded in, so the order of arrival stays visible. Two inputs pulling opposite ways, and the net is that the fifth movement's split recommendation firms up rather than softens.** **Against the urgency I argued in the fifth:** an operator-facing lever already exists and needs no code — a per-variant `MinReplicas` floor, read **before** the cheapest-at-1 clause and provably making it unreachable for any floor ≥ 1 (A67). An operator who has set a prefill floor of 2 or more is already protected from the `deadRC > 0` shed today. That genuinely bounds my *"production-reachable drain of a serving role"* framing to **unconfigured** P/D models, it is the standard configuration answer, and it belongs in the record whatever Dean decides. Two corrections that stop it being the whole answer, one mine and one the reviewer's. Mine: a floor of **1 changes no outcome here** — the shed is n → 1, precisely what the accident already yields — so the lever only bites at **≥ 2**, and "the floor already exists" without that qualifier overstates it. The reviewer's, and it is the stronger of the two: `AD5`'s premise **is** absent configuration, so asking that same operator for correct *additional* configuration gives a mitigation whose failure mode is **correlated with the bug it contains** — the models most likely to hit this are the least likely to have `MinReplicas` set. **For the fifth movement, and sharper than anything in it:** the reachable state **prices prefill**. `estimateSchedulerQueueDemand` attributes `inputTokens` to prefill and `inputTokens + outputTokens` to decode, and in the metrics-blind limit both collapse to `QueueBytes / BytesPerToken` — **equal**, carried by the one signal the outage cannot remove (A63 pass seven (x)–(z)). So the entry `VG-up` prunes in that regime is not a mis-priced entry; it is one that **correctly** carried a positive prefill requirement. Two disposition consequences follow, and they are the reason this movement exists: the fix I deferred on basis (1) — prefill demand denomination — **would not close the `deadRC > 0` regression**, so deferring the modelling work no longer implies deferring this; and the narrow asymmetric gate named in the fifth is aimed at exactly the right defect, because what needs protecting is a correctly-computed value being **discarded**, not a wrong one being repaired. **Net — the option list is three-wide and should reach Dean that way, scored:** (i) document the `MinReplicas` lever — zero code, works today, only at floors ≥ 2, and **correlated-failure coverage**, so it is a floor on severity rather than a mitigation of the regression; (ii) the narrow asymmetric gate inside PR-2's own commit — one commit plus one test, and the test **must** be built in the non-informative-with-demand shape or it lands in the inherited regime and passes on both sides; (iii) the designer's hold predicate — needs the denomination first, hence the follow-up. **I would take (i) and (ii): they are not alternatives, and (i) needs nobody's approval to write down.** The record must also carry all **three** scoping traps now on file — intra-model-only fixes leave the cross-model under-bid in place; a stale-timestamp regression test passes on both sides; and (i) is correlated-failure mitigation. **Still escalated, still not applied, still no trigger** |
| B16 | **(xxii) `AD7` / `N5` — sizing, placement, and whether it rides PR-2** | Dean has ruled the defect itself (*"2.cost: should be fixed"*): saturation reports `Cost = 0` for a zero-replica variant. Root cause is **sourcing, not arithmetic** — cost is a spec property (`va.Spec.VariantCost` is the precedent) but the pipeline reads it from `ReplicaMetrics.Cost` (`saturation_analyzer.go:59`), a live-pod-derived type a cold variant has no entry in. The addendum authorizes the fix and says where the field belongs; **sizing, placement and PR-2-vs-follow-up are explicitly mine.** ⚠ **Whichever way this goes it reverses CURRENT.md**, which lists `N5` under PR-2's *out* set — so it needs a `sync__` line either way, and that is the part that must not be silently dropped. **Note the plumbing overlap the designer flagged: one change serves three items.** `VariantReplicaState` (`saturation_analyzer.go:386-409`) is spec/deployment-derived and exists for a zero-replica variant, but carries **no `AcceleratorName` and no `Cost`** — both live on `ReplicaMetrics:58-59`, a live-pod type, which is precisely why a cold variant cannot see them. Adding those two spec-sourced fields supplies `AD7`'s cost, `AD6`'s retention-hazard accelerator identity, and the identity a sibling lookup would need — and it is the same gap the reviewer's Finding 47 hit from the other side (`fairShareRolePick` unreachable because `available[vc.AcceleratorName]` is empty). One root cause, three symptoms, so **the sizing question is really "one field pair or one item"**, which is what makes it a scope call rather than a patch. My leaning: scope the field pair together as a follow-up rather than growing PR-2, and record the three-symptom mapping so it is not re-derived a fourth time |

## C. Not in this batch — other owners

- `sync__` handoff carrying the A9/A10 recount into CURRENT.md (supersedes "all nine (6/9 subjects,
  8/9 bodies)" and "32 code/doc locations"). Blocked on the (vi)/reword-window rulings; I cannot edit
  CURRENT.md.
- Post-freeze Type-1 rationale touches: the claim-pricing rationale at `referenceVariantForRole:829-838`
  and **Finding 27**'s `:1530-1533`. The reviewer recommends deciding them **together**. Dean's, not mine.
- **Post-freeze Type-1 `(D-b)` amendment — the third such item, and the only one that is a *text
  defect* rather than a rationale touch.** `(D-b)` says to fold the one-replica ceiling into the
  per-site `headroom` computation "including its `headroom <= 0 → continue`" / "same clamp, same
  skip" / "add the ceiling to that same `break` condition". Followed literally that nests the
  ceiling inside a nil-guard at all three grant sites (A6), so the ceiling does not exist on an
  untuned variant — which is the sentinel's whole population. Correct shape is an **unconditional
  sibling** clamp, `cap = min(cap, 1 - targets[v])`, with its own `continue`/`break`, placed after
  the pool bound (A19). **Needed whichever way the C11 diff goes.** I am not amending it: Dean
  divided scope so the plan reviewer handles Type 1 and I own the derived Type 3 only, and
  post-freeze Type-1 changes go through Dean. The handoff routes the amendment to me as "Type-1
  owner" — a label the same reviewer declined for himself — so this is a genuine routing conflict
  for Dean to settle, not something to resolve by guessing.
- **`(D-a)` cannot ship as written — a Type-1 *design* defect, the fourth post-freeze item, and the
  only one that is a regression rather than a text or rationale problem.** The sentinel is written onto
  the **anchor**, and the six `PRC <= 0` gates it clears are all selection-side. Sizing is not
  selection-side: `roleBottleneckReplicas:607-616` reads the **ballot** via
  `votesFromPickerState:511-518`, which calls `prcForVariant(e.Result, …)` — the entry's own Result,
  never the anchor — and abstains on `<= 0`. Every voter abstains ⇒ `binder < 0` ⇒ bottleneck `0` ⇒
  `min(0, cap) = 0` ⇒ `deltaUtil = 0` ⇒ the model's loop breaks, taking down **every variant behind the
  admitted one**. Verified by the coder's mutation on a `[sat-not-live] + [TA]` fixture: with the
  sentinel written the *measured* variant stays at 2; with it disabled it scales past 2. The admitted
  variant gains nothing and a working variant loses its scale-up. It is the same `cap = 0` hazard
  `(D-b)`'s own ⚠ describes, arriving through the bottleneck, where the ceiling is structurally
  powerless. Corroboration: Test 10's `revived` scales *because* TA emits a PRC-only row into its own
  ballot entry; `cold` (never seen) stays 0 — sizing has always come from the ballot, and `(D-a)` is
  the only proposed admission that writes the anchor alone. **Coder held it; the tree ships `(D-b)`
  only, gates green, no golden moved.** Three-way fork, explicitly Dean's: (1) binder emits the
  sentinel into its own ballot entry — closest to `revived`, one currency, but makes a synthetic value
  a *vote*, colliding with `N8`; (2) `roleBottleneckReplicas` floors at 1 for a tagged variant —
  localised, but invents a bottleneck that ignores its voters, contradicting the abstain-not-veto
  semantics stated at `:512-517`, and needs its own gate audit; (3) narrow C11 to `(D-b)` and route
  admission to `N5` — which A20 shows the ranking correction **already** depends on. My recommendation
  is **(3)**, and by decision rather than by default: it is the only branch that preserves current
  behavior, and it lands admission and ranking together on one real fix instead of a synthetic value.
  Needs a **DEFERRED** classification with design intent, not a silent narrowing.
  **Reviewer Finding 44 makes the failure the whole reachable domain, not a point in it** (verified at
  `b6bb525c`): `aCarrier := binding; if satNR != nil { aCarrier = satNR }` (`:237-240`) plus `:211-213`
  means that when saturation binds, `binding == satNR == aCarrier` is **one pointer**, so `bByName`
  (`:260-263`) is built from the identical slice the merge iterates (`:265`) and the no-variant `else`
  is unreachable; `:230-232` returns nil when nothing qualifies. The write site is therefore reachable
  **only** with saturation present-but-not-binding and another analyzer binding-and-omitting — and in
  that state all three sub-cases are unsizable: sat `!Enabled` and sat `Enabled && !Live` never enter
  the `Enabled && Live` voting set (`:318`), while sat `Enabled && Live && !Informative` *does* vote but
  carries only NoData/Error rows at PRC 0, so the ballot-side `prc <= 0` gate skips it. `(D-a)` as
  written **cannot scale a variant from zero in any configuration.** Two consequences: (a) there is no
  *"only admit when saturation votes"* narrowing — the natural thing to reach for — because when
  saturation votes it also binds and the branch is unreachable, so that version is a feature that never
  fires and the amendment should say so; (b) **option 2 is out**, not merely weakest: in sub-case 3
  saturation *is* in the voting set pricing nothing usable, so a floor would grant a replica on the
  strength of a no-data ballot, contradicting the abstain-as-a-pricing-rule invariant. **The fork
  therefore collapses from a mechanism question to a scope question:** option 1 is the only mechanism
  that works (same state, one currency, cost = a synthetic value entering the vote — the `N8` question
  proper), and option 3 is "not in PR-2". Reviewer declines to choose between 1 and 3.
- **Mechanism now settled three ways — and the `N8` objection to option 1 is dissolved.** The designer
  (`plan__ta-anchor-da-sentinel-belongs-on-the-ballot`, 2026-08-08) **owns the defect** — *"The defect
  is **mine**. The `FZ-admission` mechanism I froze into the Type 1 at `8c2a9b04` … is insufficient as
  written"* — and independently reaches option 1: **write the `Reason`-tagged `PRC = 1` on the binding
  analyzer's own ballot entry**, letting the (b) merge carry it onto the anchor as a consequence. That
  retires my one reservation. I had scored option 1 as "a synthetic value entering the vote — the `N8`
  question proper"; the Type 1 already answers it at `:1512-1515` — the sentinel is *in the binder's
  own currency*, hence **a declared minimum, not a borrowed measurement**, which is not what `N8`
  prohibits. Anchor-only made that existing sentence false; ballot-side makes it true. So ballot-side
  is **a correction toward the frozen design's stated intent, not a new direction** — one write site,
  eligibility/ranking/bound all unchanged, and sizing works because the binder then casts
  `state[binder][role] / 1`. The arithmetic closes at exactly one replica:
  `n = min(bottleneck, 1) = 1` ⇒ `deltaUtil = 1/demand` ⇒ `k = 1`; next iteration headroom `1-1 = 0`
  ⇒ `continue` ⇒ the co-tenant is picked normally.
- **The blast radius is worse than this section recorded, and I verified every step at `2ae440e3`.**
  Two sharpenings. (1) **It is the default ordering, not an unlucky one:** saturation reports
  `Cost = 0` for a zero-replica variant (`N5`), so `0/1 = 0` sorts the never-measured variant
  **first** in `sortByCostEfficiencyAsc`. (2) **The co-tenant loses its scale-up too**, not just the
  from-zero variant. Verified chain: `PerReplicaCapacity <= 0` at `cost_aware_optimizer.go:100` no
  longer skips it ⇒ `maxTargetReplicas` → `(1, true)`, headroom 1, `allPicked` true ⇒ but
  `votesFromPickerState` (`:522-535`) reads `prcForVariant(e.Result, v)` — **the ballot, not the
  anchor** — so every entry hits `prc <= 0 { continue }` ⇒ `roleBottleneckReplicas` → `n = 0`, and
  `roleAggRemaining` (`:698-704`) abstains ⇒ `demand = 0`. The step that makes this lethal rather than
  inert is `:969-971`: **`if demand <= 0 { utilByRole[role] = 1.0 }`**, so `deltaUtil = 1.0` and the
  `deltaUtil <= 0` guard at `:982` — the guard built for exactly this — **does not fire**. Then `k` is
  computed only `if prc > 0 && demand > 0` ⇒ `k = 0` ⇒ `!anyPositive` ⇒ **`break` at `:1001`** out of
  the whole model's allocation loop. Textual corroboration in-tree: `cost_aware_optimizer.go:104-107`
  already warns that a returned cap of 0 *"would take every variant behind it down with it"* — the
  anchor-only sentinel re-opens that same hazard through the **demand** path instead of the cap path.
- **New failure mode, previously unrecorded: P/D unmatched commit.** If the never-measured variant is
  alone in its role, the *other* role's positive `k` keeps `anyPositive` true, so there is no break —
  instead prefill commits while decode stays at zero, violating the matched-joint-commit that
  `allocateForModelPaired` (`:930`) exists to guarantee. The break is loud; this one is silent.
- **Dean's *"use sat's demand"* option is now closed with a reason, not merely ranked last.** TA binds
  only when saturation fails `Enabled && Live && Informative`, and `votingResults` (`:332-340`) prunes
  on `Enabled && Live`: failed-on-Live ⇒ not in the voting set at all (no donor exists);
  Live-but-not-Informative ⇒ every variant capacity is a no-data/error sentinel ⇒ `prcForVariant <= 0`
  ⇒ abstains. *"There is no saturation demand to borrow at the moment TA binds."* `N8` rules against
  cross-analyzer borrowing independently. Break→skip is also ruled out as the quick fix: *"fixes the
  blast radius but not the feature"*, and it needs a per-iteration exclusion set or the re-pick spins.
- **My scope recommendation is unchanged — defer — but the reason changed and it now carries a
  mandatory rider.** Not "because option 1 collides with `N8`" (dissolved above) but because it is a
  feature addition to a PR sitting on its last commit, and **today is safe**: `V_zero`'s anchor PRC is
  0, so `:100` passes it over and the co-tenant is picked normally — *"there is no live bug to bypass
  today."* The rider is the designer's own, and is the part that must not be dropped: whichever
  disposition Dean picks must be **chosen explicitly**, and *"if it stays deferred, the follow-up must
  carry the ballot-side requirement, not just 'write the sentinel'; otherwise a future implementer
  reads my Type 1 and builds the broken version."* Concretely that makes the deferral a **§4b DEFERRED
  classification with a mechanism clause**, not a one-line TODO — a Type-3 row I own and can apply in
  the batch. **What I am not deciding:** whether anything lands in PR-2 (Dean's), and the Type-1
  amendment itself (the designer's).
- **Designer's queued Type-1 amendments — no action for me, but they change what the Type 3 derives
  from.** `:1524-1526` marks eligibility as "the whole point" (*"the located error: necessary treated
  as sufficient"*); `:1512-1515` gains the ballot-side precondition; the `:2062` roll-up's *"leaving
  nothing open on either"* is wrong; the `FZ-admission` findings row's *"Adopted — folds into PR-2"*
  overstates. Plus one broader than `(D-a)`: **the `N2`/`N7` Disposition cells still read "Open"
  although both are Dean-confirmed RULEs that shipped** — *"the Disposition column needs a sweep
  against the branch, not spot fixes."*
- **Fourth argument for ballot-side, and a P/D correction to the row above.** With `Role` carried
  correctly the ballot-side sentinel needs **no P/D-specific mechanism** — the joint-commit math does
  it: the from-zero variant is picked for its real role, capped at 1 by C11, the binder prices it so
  `demand > 0`, and `deltaUtil = min` over roles propagates the one-replica ceiling **proportionally to
  the partner role**, yielding a matched 1P+1D step. Under the **anchor-only** shape, though, P/D is
  *worse* than the unmatched-step failure recorded above: every ballot entry abstains
  (`votesFromPickerState:522-535` reads `e.Result`, not the anchor) ⇒ `n = 0` for decode ⇒ `k = 0` ⇒
  **decode's `remaining` never decrements** while prefill keeps drawing against zero decode replicas
  until `!anyPositive` breaks. **The roles decouple completely** — correct "unmatched single step" to
  "anchor-only decouples P/D" wherever that framing was recorded, including in the bullet above.
- **Designer's §3 — a broader latent hazard, its finding, not carried by me.** The blank-`Role` bug is
  only one way to trip `initRoleState`. Verified at `:390-397`: the `else` branch synthesizes
  `domain.RoleBoth` into the same `roleSet` for **any** entry with `RoleCapacities == nil`. So the
  invariant is *"on a disaggregated model, any voting entry that does not report per-role
  `RoleCapacities` injects `both` into `roles`, and the first `pick("both")` kills the model's
  allocation."* `roles` is derived from the **ballot**; satisfiability is decided against the
  **anchor**; nothing reconciles them — the same ballot-vs-anchor split as `(D-a)`, but a *key set*
  rather than a value. **`a38d7b73` does not close this** — it fixes the one blank-`Role` source, not
  the derivation — so A36's rebase leaves the general hazard open for any future analyzer. Not
  reachable today (TA and saturation both report `RoleCapacities` on P/D; the QM analyzer is parked).
  The designer notes this is its `N7`, recorded only in the scale-**down** direction, and that the
  scale-**up** direction is strictly worse — *"not a skipped role, a dead model"* — and that the
  `break` must **not** become a skip, since it is load-bearing for joint-commit.
- **Errata, and why it matters more than the two numbers.** A sibling handoff
  (`plan__ta-anchor-da-sentinel-errata-two-line-refs`) corrects two refs the designer could no longer
  edit once I marked the first `.WIP`: `cost_aware_optimizer.go:103-107` → **`:104-108`**, and
  `votingResults` `:315-323` → **`:332-340`**. Drift under the coder's `2ae440e3`; code unchanged in
  both. **My independent read agrees with the corrected refs, not the originals** — that is the check
  that matters, and the substance survives the drift intact. Self-reported cause: citations written
  from an earlier `git show HEAD:` dump but labelled with a stale SHA — *"a dump is only valid for the
  SHA you read it at"* — the same tip-staleness class I had flagged at it in
  `designer__t1-1-not-shipped-and-pending-edits-exists`. Sender rule held correctly: it stopped
  editing at `.WIP` and sent a sibling rather than amending in place.
- **Shipped production-prose defect, same amendment.** `analyzer_helpers.go:185-188`'s premise is false
  (`ResultIsInformative` is any-variant per `:57-62`, so a healthy binder can be informative in
  aggregate yet price nothing for one variant — the expected shape for a never-measured from-zero
  variant), *and* the sentence is incoherent about its subject: "a binder omits a variant only when the
  binder itself is … not-binding". Conclusion holds; re-justify on `:189-192`. Note this is **new PR-2
  prose**, which per A10 is the first production `.go` doc comment surface this mission touches.
- Routing: who owns post-freeze Type-1. The internal code reviewer has declined the "Type-1 owner"
  label I used in two handoffs; that error is also now in shipped code (see B3).

## Provenance

Handoffs feeding this batch, all read at their stated states:
`plan__ta-anchor-c6e-two-adjacent-defects.md.WIP`, `plan__ta-anchor-c6f-w4-no-spend-is-false.md.WIP`,
`plan__ta-anchor-claim-pricing-verdict-and-c6e-gap.md.WIP`,
`plan__ta-anchor-c11-ceiling-nil-maxreplicas-escape.md.WIP` (Findings 42/43, pre-registered at
`470f4b8d` *before* the C11 diff existed); reviewer write-up
`planning/ta-anchor-dynamic-refresh-review.md` @ `ded9dc5f`. Branch tip when the A19/A6-widening rows
were added: `eb12089a`, with **six** source files modified and uncommitted (C11 in flight, touching
all three grant sites).

Later handoffs: `plan__ta-anchor-c10-import-cycle-blocker.md.WIP` (Finding 48) ·
`plan__ta-anchor-ceil-divergence-and-handoff-reconciliation.md.WIP` ·
`plan__ta-anchor-c10-shipped-supersedes-my-layering-rec.md.WIP` ·
`plan__ta-anchor-pr2-c9-sweep-inputs.md.WIP` (Findings 49/50 + the corrected ledger + the §4 ruling
request; review write-up commits `dba35c60`, `34f3feab`, `e498c5d6`). **Branch tip for every row added
from that last handoff: `79a590d6`** — twenty commits on base `075a208e`, C10 landed at `1a50b418`.
A22's tip correction is therefore now **two** commits further stale than A22 itself records
(`d9f3b97e` → `b6bb525c` → `1a50b418` → `79a590d6`); apply A22 against the tip at approval time, not
against any SHA written here.

Designer round on the `(D-a)` sentinel (A35–A37): `plan__ta-anchor-da-sentinel-belongs-on-the-ballot.md.WIP`,
`plan__ta-anchor-da-sentinel-errata-two-line-refs.md.WIP`,
`plan__ta-anchor-pd-fix-is-one-line-already-on-main.md.WIP`,
`plan__ta-anchor-designer-withdraws-t1-1-and-the-coder-accusation.md.WIP`. Reviewer follow-up (A38):
`plan__ta-anchor-pd-phantom-bucket-second-effect.md.WIP` — from **review**, not the designer;
`ask: fyi`, disposition explicitly left to me. Branch tip for A38: **`2ae440e3`** (C9b landed; C9c in
flight as an untracked `optimizer_invariant7_test.go`).

§4a sweep round (A43–A45, B12–B13): source was **`ta-anchor-dynamic-refresh__review-findings-58-59.md`**, a
trigger addressed to the **coder**, not to me — read for context only, state left untouched, and it
carries no instruction to me by design. Findings 58/59 are recorded in the reviewer's
`planning/ta-anchor-dynamic-refresh-review.md`. **Branch tip for every row in this round: `4e369f10`**
— two commits past the `2ae440e3` the previous round used, both landed in the interval: `209e148f`
(C9c, which *tracked* the previously-untracked `optimizer_invariant7_test.go`) and `4e369f10` (C9d).
Finding 58 verified exact; Finding 59's total corrected 35/11 → **36/12** while its delta (13 added by
`209e148f`) verified exact. A45's mandate/mapping check is complete; its per-entry literal
transcription is deliberately **not** verified and is flagged in the row as the one thing to check
before push.

**Code-complete round (A46–A52, B14; corrections to A43/A44 and the B10 reversal) — 2026-08-08.**
Sources: `plan__ta-anchor-pr2-code-complete-c9-closeout.md` (addressed to me, marked `.WIP` by me,
processed here) and `plan__ta-anchor-c6c-ceil-eviction-fork.md` (also mine, `.WIP`, and the reason
B10 is reversed — its item 1 carries the measurement). **Branch tip for every row in this round:
`a9afb740`** — C9e, *"docs: make every reference in shipped comments resolvable from main"*; **25
commits on base `075a208e`, working tree clean, nothing pushed.** C9 decomposed by the coder into
C9a `757fc6f5` · C9b `2ae440e3` · C9c `209e148f` · C9d `4e369f10` · C9e `a9afb740`.

**Three of my own rows were corrected in this round, all by checking the tree rather than re-reading
the handoff** — worth recording as a pattern, since two of the three were mine and one was the
reviewer's: (1) **A44's framing retracted** — `T1-ols` is `itlReasonT1OLS`, a shipped product
identifier, so its 36 occurrences are a §4a carve-out and the count governs nothing; found by asking
why C9e skipped all 36 rather than assuming its sweep was incomplete. (2) **A43's completeness claim
narrowed** — my shape-(ii) pattern missed the `TA-*.md` family and so missed `constants.go:85`;
the PR-2 single-site conclusion survives, the "fully enumerated" claim does not. (3) **B10 reversed, then resolved back** — I had recommended `floor` on unit-consistency grounds;
the coder's C6c measurement (9/334, multi-replica deltas, `bv` 6→2 under an unconstrained pool)
plus the pool-vs-entitlement asymmetry moved me to `ceil`; review then showed that measurement
**predated its own mitigation** (C6e's `firstDraw` guard, two commits later), and the refreshed
measurement at HEAD — 5 of 386, worst −2, **zero** eviction rows, both C6c-era worst rows green
— withdrew the evidence I had departed from the authority on. Net: **`floor`, as the frozen Type 1
says**, with the asymmetry retained as a design note rather than a fork argument. Recording it as
two moves rather than one because the shape matters: a position held on a measurement should move
when the measurement is withdrawn, and the thing that made this checkable was that *review priced
the input's age rather than re-arguing the conclusion*. Also recording the half of my A54 prediction
that was **falsified** — I predicted surviving collapses among models already holding replicas, and
there were none; the code reading behind it stands, the population estimate did not. **A38's hazard is CLOSED** by the coder's §2 verification on three
independent axes — different package, not on `Optimize`'s call path with literal `RoleCapacities`;
every P/D fixture variant carries an explicit `Role`; no P/D fixture has a zero `ReplicaCount`. The
flag did its job before C9c froze anything, which is the whole point of raising it when it was in
flight.

**Status distinction that must survive into any report: PR-2 is CODE-COMPLETE, not push-ready.**
Every plan commit including all of C9 has landed and all gates are green (`gofmt`, build, vet,
`make test`, `-race`, `make lint` 0 issues), and the coder states *"Nothing is pushed and I have not
asked to push."* CODER-CONVENTIONS §5.4 requires the internal code review **before** push
authorisation, and `review__ta-anchor-dynamic-refresh-checklist.md.HOLD` is still on HOLD by Dean's
instruction. Two further gates are unaffected by code-completeness: the `make lint` green was
obtained under golangci-lint **2.8.0**, and `main` has since moved to **2.10.0** (PR #1512), so it
must be re-run after any rebase; and PR-1 has merged, so PR-2's rebase target is now plain
`main@57f3fe64`+ rather than the pre-merge tip it is stacked on.

**Reword-ledger correction round (A53) — 2026-08-08.** Source:
`plan__ta-anchor-pr2-c9e-message-is-on-the-reword-list.md` (addressed to me; `.WIP` → `.DONE` by me).
**Branch tip unchanged at `a9afb740`, still 25 commits on `075a208e`, working tree clean, nothing
pushed** — Findings 60–63 produced **no commit**, correctly: 60 is a reviewer-instrument defect, 61 and
63 are inherited and not PR-2's, and the one actionable item (`a9afb740`'s own message) needs history
rewriting, which is not the coder's. Reviewer Findings 60–63 read directly at
`planning/ta-anchor-dynamic-refresh-review.md:6819-6885`; they corroborate A46 and A47 and add no new
plan row of their own. The handoff's two corrections were **already independently in A46**, which is
the useful signal — the coder and I converged on the same two errors from different directions within
the hour. What is new is A53: its concession of `b106b929` is one message too many, refuted by the
criterion `a9afb740`'s own body states. **Carry 22 of 25, not 21.**

**Fork re-measurement round (A54, A55; B5/B7/B10 revised) — 2026-08-08.** Sources, both addressed to me:
`plan__ta-anchor-fork-priced-before-its-mitigation.md` (review — the input is stale) and
`plan__ta-anchor-ceil-floor-remeasured-at-head.md` (coder — the refreshed number). **Branch tip
unchanged at `a9afb740`, 25 commits on `075a208e`, working tree clean, nothing pushed.** Mid-round I
found the coder's tree live-edited (` M greedy_score_optimizer.go`, `:837` mutated to `math.Floor`
under a doc comment still reading *"rounding up"*) and restricted every further read to `git show
HEAD:`, ran no test or build there, and touched nothing — the measurement was in flight and has since
been reverted, which I re-verified. **Net movement: one decision left the queue.** The `ceil`/`floor`
fork resolves to **`floor`** — the frozen Type 1 as written — so B10's reversal is withdrawn, B7's
divergence resolves by the *code* moving, and B5 loses two of its three options. **The round's own
lesson is about instrument discipline, not about rounding:** review did not re-argue my conclusion, it
priced the *age of my evidence*, which is what made the error findable at all. My A54 correction to
review's mechanism reading was right about the code (`allocated` is per-call, `firstDraw` is keyed on
cumulative `targets`) and **wrong about the population** — I predicted surviving collapses and the
measurement found none. Reading a mechanism and predicting its reach are different acts. **What did not
close:** A55's residual is now the largest single under-report in this whole pattern — the round-up
endorsement lives in **six comment blocks** that no `ceil` grep can see, so C6c's prose sweep has to be
specified by line. Fifth consecutive round in which an instrument certified exhaustive was
under-inclusive at its own boundary.

Two claims in an earlier round were verified against the object and **corrected**, both in ways that matter:
the designer's `Role: vs.Role` is actually **`Role: st.role`** (persisted state, not the loop variable
— which voids shape 2's "clean no-op at rebase" safety case, since the text differs), and its "stale by
one" was actually **four** commits, meaning **C10 had already landed** when the plan text and CURRENT.md
still called §2e pending. The reviewer independently confirmed both. Treat every line ref and staleness
count arriving by handoff as a claim to check, not a fact to copy — the substance survived each time,
the coordinates did not.
